// 파셀 배치 골든 스냅샷 — **리팩터 안전망**.
//
// ── 왜 성질 테스트만으로는 부족한가 ──────────────────────────────────────────
// `world2-parcel-layout.test.ts`는 배치의 *성질*을 본다 — 결정론이다, tier 포함관계가
// 성립한다, 셀 경계를 안 넘는다. 그 성질들은 배치 규칙을 다시 써도 **여전히 성립한다.**
// 즉 "같은 입력 → 같은 출력"은 검사하지만 "리팩터 전후 같은 출력"은 아무도 안 본다.
//
// 그래서 구조를 바꿀 때는 그것만으로 부족하다. 난수 소비 순서가 한 번만 어긋나도 세상
// 전체가 다른 모습이 되는데, 위 테스트는 전부 통과한다. 눈으로 봐야 알고, 눈으로 보면
// "달라 보이는데 원래 저랬나?"에서 멈춘다.
//
// 여기서는 배치 결과 자체를 고정한다. 파츠 레지스트리로 옮기는 작업이 **행동 무변경**임을
// 이 파일이 증명했다. 배치를 의도적으로 바꿀 때는 이 값을 함께 갱신하는 것이 맞다 —
// 그때 갱신은 "바꿨다"는 명시적 선언이 된다.
//
// ── 갱신 이력 ────────────────────────────────────────────────────────────────
// · 36,459 → 37,164 — **도로 폭 축소**(감독 지시 2026-07-31 *"길이 너무 넓은것같아"*).
//   `ROAD_SEG` 8 → 5. 8m 는 차도 폭이었는데 이 세계에는 차가 없다. 같은 커밋에서
//   `ROAD_HALF`·`SETBACK` 을 유도로 바꿔(*"하드코딩하지말고"*) 셋백이 7 → 6 이 됐고,
//   가로등 자리(`LAMP_OFFSET`)도 인도 중점 유도라 6.25 → 4.5 로 따라왔다.
//
//   **개수가 는 것은 나무 +615 · 벤치 +32 · 화분 +58 셋뿐이다**(합 +705, 총계와 일치).
//   셋백이 1m 물러나며 생긴 여유를 그것들이 가져갔다. 나머지는 전수 대조로 무변경 —
//   도로 15,651(조각 **수**는 그대로이고 폭·길이만 바뀌었다) · 건물 4,587 · 가로등
//   6,894 · 지면 1,875 · 정원 1,785 · 시계탑 3 · 분수대 2.
//
//   대표 파셀에서 건물 3채의 **크기·회전·톤이 소수점 여섯 자리까지 그대로**이고 위치만
//   원점 쪽으로 밀렸다 — 셋백만 건드렸다는 증거다. 나무가 2 → 3 그루가 된 것이 위
//   +615 가 파셀 단위로 나타난 모습이다.
// · 36,459 (해시만 변경) — **지면 두께가 바다 수면에서 유도되도록** 바뀌었다(검수관
//   블로커). `GROUND_DEPTH` 가 `0.8` 상수였는데 감독이 바다를 −1.0m 로 내리자 판이 수면
//   보다 얕아져 바닷가에서 옆면이 20cm 드러났다. 이제 `|SEA_Y| + 0.3` 이다.
//   **부품 수와 위치는 한 자리도 안 바뀌었다** — 바뀐 것은 `ground` 한 항목의 `sy` 뿐이고,
//   그것이 해시에 들어가 있다. 대표 파셀에서는 그 자리를 `GROUND_DEPTH` 로 유도하도록
//   고쳤으므로, 다음에 수면을 옮기면 대표 파셀은 저절로 따라오고 해시만 갱신하면 된다.
// · 36,943 → 36,459 — **건물이 파셀 경계를 넘던 것**(가로등 여유를 유도로 확인하다
//   드러났다). `footprint` 는 `반변 + 0.6`(처마 몫)인데 배치 한계는 반변만 당겨서,
//   이웃 파셀 건물과 서로 0.6m 씩 뻗어 **최대 0.74m 겹쳤다**(13×13 파셀에서 11쌍).
//   감독이 반복 지적한 *"겹쳐져 있는 것들이 보여"* 의 남은 몫이다.
//   그 0.6 을 `EAVE` 상수로 뽑아 겹침 반경과 배치 한계가 **같은 값**을 보게 했다.
//   건물 도달 반경 16.599m → **15.999m** 로 경계(16m) 안에 정확히 들어온다.
//   **건물 수는 4,587 그대로**다 — 위치만 안쪽으로 밀렸고, 그만큼 슬롯을 더 차지해
//   나무 −378 · 벤치 −42 · 화분 −64 가 줄었다. 도로·지면·정원·가로등은 무변경.
//
//   ── 왜 어느 검사에도 안 걸렸나 ────────────────────────────────────────────
//   겹침 검사가 전부 **파셀 안**만 봤다(41×41 전수로 0). 파셀 경계를 넘는 겹침은 볼
//   지점이 아예 없었다. `world2-parcel-slots.test.ts` 에 세계좌표 전수 검사를 새로
//   넣어 그 사각을 닫았다.
// · 42,540 → 36,943 — **가로등을 교차로에서 블록 중간으로**(감독 지적 *"왜 가로등이
//   2개 한쌍처럼 보이지?"*). 도로 팔 중간(cell/4 = 8m)에 양옆으로 세우니 교차로
//   모서리에서 동서용 `(8, 6.25)` 와 남북용 `(6.25, 8)` 이 **2.47m** 로 붙었다.
//   자리를 **파셀 경계**(16m)로 옮기고, 겹치지 않게 `+`방향(east·south) 경계만 자기
//   것으로 삼는다 — 나머지 둘은 이웃이 자기 `+` 로 세운다.
//   가로등이 13,866 → 6,894 로 절반이 됐고(간격 16m → 32m, 실제 도시가 25~40m다),
//   빈 자리를 나무 +1,185 · 화분 +116 · 벤치 +74 가 가져갔다.
//   **도로·건물·지면·정원·랜드마크는 한 개도 안 변했다**(종류별 전수 대조). 이 변경이
//   가로등과 그것이 비우던 자리에만 닿았다는 뜻이다.
//
//   ── 폐기한 처방을 남겨 둔다 ────────────────────────────────────────────────
//   중간에 **모서리마다 주인을 정해 한 대만** 세우는 안을 구현해 테스트까지 통과시켰다
//   (골든 36,174). 파셀 하나만 보면 멀쩡한데 이웃까지 이어 보면 x축 도로를 따라
//   `(−8,−6.25) · (8,+6.25) · (24,−6.25)` 로 **좌우 지그재그**였다 — 붙음을 고치려다
//   *"길 양옆에 가지런히"* 를 도로 깬 것이다. 골든 숫자만 갱신하고 넘어갔으면 그대로
//   나갔다. **파셀 단위 검사로는 볼 수 없는 결함**이라, `world2-lamp-placement.test.ts`
//   의 검사를 파셀이 아니라 **경계** 단위로 다시 썼다.
// · 33,296 → 42,540 — **가로등을 far tier 까지**(나무와 같은 이유). near 경계가
//   1.15셀 = 36.8m 인데 안개는 51.2m 부터라, 밤에 불빛 두 줄이 또렷한 거리에서 툭
//   끊겼다. 길 양옆으로 세운 직후라 더 그렇다 — 가지런함이 반만 보인다.
//   늘어난 9,244개가 전부 mid·far 파셀의 가로등이다. **near 배치는 한 자리도 안
//   바뀌었다**(대표 파셀 스냅샷이 그대로인 것이 그 증거다).
// · 31,436 → 33,296 — **가로등을 길 양옆으로**(감독 재지적 *"가로등은 길 양옆에
//   가지런히 있어야하는데. 무슨 나무처럼있냐"*). 앞서 난수 배치를 도로 축으로 옮기며
//   **한쪽에만** 세웠다 — `road-topology.ts` 주석이 *"비키는 쪽을 축마다 고정한다"* 고
//   적어 둔 그것이고, 줄이 길 건너로 옮겨 다니는 것을 막으려던 처방이 과했다. 양쪽에
//   각각 세우면 두 줄이 각자 이어지므로 건너뛰지도 않고 길 양옆이 채워진다.
//   방향당 1 → 2 이므로 늘어난 1,860개가 전부 가로등이다. 사거리 파셀은 8대가 된다.
// · 29,913 → 31,436 — **나무를 far tier 까지**(감독 지적 *"lod 가 자연스럽지 않다.
//   이동하면 멀리 있는 게 사라졌다가 멈추면 나타나. 특히 나무."*). mid 경계가
//   1.55셀 = 49.6m 인데 안개는 51.2m 부터라, 나무가 **안개보다 앞에서** 사라져 가릴
//   것이 없었다. far 끝(2.4셀 = 76.8m)은 안개 끝과 같으므로 거기서 사라지는 것은
//   안개 100% 에 묻힌다 — 전환을 없애는 대신 **볼 수 없는 곳으로 옮겼다.**
//   늘어난 1,523개가 전부 far 파셀의 나무다. **near·mid 배치는 한 자리도 안 바뀌었다**
//   (대표 파셀 스냅샷이 그대로인 것이 그 증거다).
// · 32,274 → 29,913 — **파셀 안 겹침 제거**(감독 지시 *"나무 건물 다른 것들 가로등,
//   겹쳐져 있는 것들이 보여. 구획을 잘 배치해서 균형, 통일감 있게 보이게 해줘."*).
//   파츠가 서로를 모른 채 각자 자리를 뽑던 것을, 파셀이 슬롯 목록을 갖고 파츠가 빈
//   것을 가져가는 방식으로 바꿨다(`decide/parcel-slots.ts`). 건물 최대 변도 8 → 6 으로
//   줄였다 — 사분면이 6.5m 인데 8m 건물이 제 구획을 넘어 도로와 가로등을 덮고 있었다.
//   **개수가 준 것은 자리가 없어 덜 놓였다는 뜻이다**(겹쳐 서느니 덜 서는 쪽을 골랐다).
//   겹침이 실제로 0인지는 `tests/world2-parcel-slots.test.ts` 가 41×41 × 3 tier 전수로
//   확인한다 — 이번엔 조사로 끝내지 않고 검사로 남겼다.
// · 해시만 변경(개수 32,274 그대로) — **지면 판 두께 0.1 → 0.8m**(감독 지적 *"강과
//   바다 땅보다 더더 아래. 50센치 밑에있게해줘"*). 물은 이미 아래에 있었는데
//   (`SEA_Y` −1.2m) 화면에서 붙어 보인 원인이 이 값이었다 — 물가에서 보이는 것은 육지
//   파셀의 **옆면**뿐인데 그것이 10cm 라 원근상 없는 것과 같았다. 당시 `SEA_Y` 를 −0.5m 로
//   (그 값은 2026-07-30 감독 지시로 강 −0.5 / 바다 −1.0 으로 갈렸다 — 지금 −0.5 에
//    해당하는 것은 `RIVER_Y` 다)
//   맞추고 판을 그보다 깊게 만들어 50cm 단차가 눈에 보이게 했다. **개수가 그대로인
//   것이 판의 크기만 바뀌고 배치는 안 건드렸다는 증거다.**
// · 해시만 변경 — **바닥 판 높이**(정원 0.03→0.07, 도로 0.06→0.14). 세 판이 6cm 안에
//   겹쳐 있었다. 깊이 버퍼는 먼 거리일수록 정밀도가 떨어져 그렇게 촘촘하면 어느 쪽이
//   앞인지 못 정하는 지점이 생긴다. 벌리는 비용은 0 이다 — 눈높이 1.7m 에서 7cm
//   단차는 안 보인다.
// · 해시만 변경(개수 31,148 그대로) — **도로 조각 겹침 제거**(감독 판정 *"길이 겹쳐지는
//   곳. 사거리? 시선을 밑을 보고 미세하게 움직이면 거기는 왜 텍스처가 울어."*).
//   팔 시작 공식이 `t = i × SEG − SEG/2` 라 첫 조각이 중심 조각과 4m 겹쳤다. 중심
//   반폭부터 파셀 변까지를 조각 둘로 나누도록 바꿨다. **개수가 그대로인 것이 조각
//   수는 안 건드리고 위치·길이만 고쳤다는 증거다.**
// · 36,503 → 31,148 — **정원을 사분면 넷에서 통짜 한 장으로**(감독 판정 *"건물, 나무
//   있는 곳 바닥 정리가 필요해. 뭔가 건물이 어긋나있네."*). 파셀당 정원이 4 → 1 이라
//   3 tier × 파셀 수만큼 줄었다. 건물·나무는 정원 판과 무관하게 배치되므로 판을 가로질러
//   서거나 판 사이 틈에 발이 놓였고, 화면에서 초록 조각이 어긋나 흩어져 보였다.
//   통짜 한 장이면 어디에 서든 발밑이 잔디다.
//   같은 커밋에서 가로등·벤치·화분을 world1 실물로 되살렸지만 **개수는 안 변했다** —
//   조각이 여럿이어도 `bakePieces` 로 한 지오에 굽기 때문이다.
// · 해시만 변경(개수 36,503 그대로) — **나무 비율·크기·회전**(감독 판정 *"나무가 위로
//   길쭉해. 포토샵으로 위로 길게 늘린 느낌. 그리고 나무 크기를 똑같이 할 필요는 없지."*).
//   `sy = s × (1.2~1.8)` 로 세로만 늘리던 것을 `× (0.94~1.06)` 으로 좁히고, 다양성을
//   크기 자체(0.6~1.9)로 옮겼다. 회전도 90° 단위에서 자유로 풀었다.
//   **개수가 안 변한 것이 이 변경이 스케일만 건드렸다는 증거다** — 나무가 몇 그루
//   서는지는 그대로이고 각 그루의 크기·회전만 달라졌다.
// · 29,363 → 36503 — **정원**(감독 지시 *"바닥 격자 바닥이 잘 드러나지 않아. 도로는 지금
//   수준으로. 건축 올릴 수 있는 땅은 정원으로 표시해."*). 파셀마다 사분면에 잔디 판
//   하나씩, 광장 제외. 도로는 손대지 않았다 — 격자가 안 드러나던 이유가 지면과 도로의
//   명도 차이라서, 도로를 그대로 두라는 지시대로 **땅 쪽을 밝혔다.**
//   판 크기를 셋백(7m)이 아니라 도로 경계(ROAD_HALF 5.5m) 기준으로 잡았다. 처음 셋백
//   으로 하니 4.5m 정사각형이 되어 격자를 드러내기엔 작았다 — 셋백은 *건물이 물러서는
//   선* 이지 땅의 경계가 아니다. 7.5m 면 파셀 면적의 22%가 정원으로 보인다.
// · 28,193 → 29,363 — **world1 소품 이식**(감독 지시 *"월드 1의 나무와 소품들 가지고
//   오고"*). 벤치(near·mid)와 화분(near) 두 파츠를 넣었다. world1 에서 가져온 것은
//   **치수뿐이다** — 좌석 높이 0.45m 처럼 라이브에서 검증된 값은 계승하되, 파셀 단위
//   `mergeGeometries` 조립은 안 가져왔다. world2 는 종류당 InstancedMesh 하나라 병합이
//   필요 없고, 병합하면 파셀마다 새 지오가 생겨 개수 불변식이 깨진다.
//   대표 파셀 (3,-7,near) 에는 화분 하나가 늘었다(벤치는 확률 35%에 안 걸렸다). 나머지
//   19항목의 좌표가 소수점 여섯 자리까지 그대로다.
//
//   ── 근거를 바로잡는다 (검수관 지적) ──────────────────────────────────────
//   위 두 갱신에서 "대표 파셀 하나가 통과하므로 나머지도 안 바뀌었다" 고 적었는데,
//   **귀납으로는 약하다** — 900칸 중 1칸이 같다고 899칸이 같다는 보장은 없다. 이
//   프로젝트가 "한 표본의 통과를 성질의 증거로 쓰는" 사고를 이미 겪었고, 같은 형태다.
//
//   진짜 근거는 표본이 아니라 **구성**에 있다:
//   · 광장 — `parts/plaza.ts` 의 `isPlaza` 는 `if (isCentralPlaza) return true` 를 앞에
//     얹기만 했다. 중앙광장이 아닌 모든 좌표는 실행 경로가 diff 전후 **동일하다.**
//   · 파츠 추가 — `decide/parcel-layout.ts` 가 종류마다 `hash2(base, spec.salt)` 로
//     독립 시드를 쓴다. 배열에 새 종류를 넣는 것이 기존 종류의 난수 소비를 **건드릴 수
//     없다.**
//   대표 파셀은 그 구성 논증을 뒷받침하는 보조 확인이지 증명이 아니다.
// · 28,358 → 28,193 — **중앙 광장 고정**(감독 지시 *"중앙은 중앙광장으로 하고 분수대
//   시계탑"*). 둘이 함께 반영됐다. (a) 원점 둘레 3×3 아홉 칸이 무조건 광장이 되어 그
//   칸의 건물·도로중심 조각이 빠졌다. (b) 분수대·시계탑이 **동네 광장마다 확률 반반으로
//   서던 것을 중앙 광장에만** 세웠다 — 두 블록마다 분수대가 있으면 중앙이 특별할 이유가
//   없고, 그건 랜드마크가 아니라 가로수와 같은 반복 요소다.
//   광장 아닌 곳은 안 건드렸다는 증거는 아래 **대표 파셀 (3,-7,near) 테스트가 그대로
//   통과한 것**이다 — 건물 3채·나무 3그루·가로등 3개의 좌표가 소수점 여섯 자리까지
//   같다. 숫자만 갱신하면 이 안전망이 그 순간 장식이 되므로 근거를 함께 남긴다.
// · 23,525 → 28,358 — **도로 격자화**(감독 지시 *"길을 변경하자. 월드 1처럼 격자로"*).
//   경계마다 확률 0.6 독립 추첨이던 것을 `decide/grid.ts` 의 격자 판정으로 바꿨다.
//   예전에는 활성 경계 수가 B(4,0.6) 이라 평균 2.4방향이었는데 이제 대부분 사거리라
//   파셀당 도로 조각이 늘었다(+4,833 = 파셀당 평균 2.6조각). 늘어난 것은 **도로뿐이다** —
//   대표 파셀 (3,-7,near) 에서 건물 3채·나무 3그루·가로등 3개의 좌표가 소수점 여섯 자리까지
//   그대로다. 도로 방향은 건물 배치의 입력이므로 위치가 바뀔 수도 있었는데, 사분면 1:1
//   배정이 방향과 무관하게 동작해 그렇지 않았다. 격자화가 도로만 건드렸다는 증거다.
// · 23,490 → 23,525 — 랜드마크 가시 반경 확대. 분수대 near→mid(56m), 시계탑
//   near→far(76.8m). near 전용(41.6m)이면 광장이 평균 115m 간격이라 **서 있는 자리에서
//   보일 확률이 11.5%** 였다 — 감독이 "광장 안 보여 / 시계탑도 안 보이던데" 로 잡았다.
//   부품 수가 조금 는 것은 mid·far 로도 그려지게 됐기 때문이다.
// · 23,676 → 23,490 — 광장 배선. 광장 파셀은 건물이 0이고 도로 중심 조각이 빠지며,
//   그 자리에 분수대나 시계탑이 하나 선다. 순증감이 작은 것은 광장이 1.9% 뿐이라서다.
// · 37,164 → 37,146 — **고층 타워 파츠 신설**(감독 지시 2026-08-02, 스카이라인).
//   `tower` +48(파셀 16곳 × 3 tier), `building` −66. 순 −18 인 것은 타워 파셀에서
//   일반 건물을 통째로 뺐기 때문이다 — 타워는 파셀 **중앙**을 쓰는데 건물은 사분면에서
//   자리를 뽑아 안쪽이 겹친다. 겹침 판정(`footprint`)은 "앞선 것을 피한다" 이지
//   "뒤엣것을 밀어낸다" 가 아니므로 판정 단계에서 갈라야 한다(`parts/zoning.ts`).
//   25×25 격자에서 타워 파셀은 16곳(2.6%) — 희소해야 랜드마크다.
// · 37,164 (해시만 변경) — 지면 두께 `GROUND_DEPTH` 가 바다 수면(−1.0) 대신 **해저**
//   (−3.4)에서 유도되게 바뀌었다(1.3 → 3.7). 감독 실기기에서 물속 시점이 열리자 판
//   아랫면과 해저 사이 2.1m 가 뚫려 있었다 — 육지가 "떠 있는 판" 으로 보이고 그 밑으로
//   헤엄쳐 들어간다. **부품 수도 위치도 안 바뀌었다**: 새 배치에서 `ground` 의 `sy` 만
//   1.3 으로 되돌리면 옛 해시 `e18380ae…` 가 소수점 여섯 자리까지 정확히 재현된다
//   (실측으로 확인했다 — 해시가 통째로 바뀌면 "그것만 바뀌었다" 를 눈으로 못 보므로).
// · 26,568 → 23,676 — 건물 밀도 완화. 채수 2~6 → 1~4, 사분면 1:1 배정, 셋백 7m.
//   감독 지적 "건물이 빽빽하다" 의 처방이다.
// · 26,568 (해시만 변경) — 도로 `tone` 이 0·1 무작위에서 0 고정으로. 부품 수와 위치는
//   그대로다. 텍스처가 있는 파츠의 `tones` 는 색이 아니라 곱셈기라서 흰색 하나만 둔다
//   (`parts/road.ts` 참고 — 어두운 톤이 텍스처와 곱해져 길이 검게 나왔다).
// · 15,660 → 26,568 — 도로 추가. 도로 자체(파셀당 평균 5.8조각)와, 건물·나무·가로등이
//   길을 피해 사분면에서 자리를 뽑도록 바뀐 것이 함께 반영됐다. 후자는 부품 수를 바꾸지
//   않고 위치만 옮긴다.

import { describe, it, expect } from 'vitest';
import { createHash } from 'node:crypto';
import { parcelLayout, type PlacedPart } from '../frontend/js/world2/decide/parcel-layout.js';
// 지면 두께는 바다 수면에서 **유도되는** 값이다(`parts/ground.ts`). 아래 대표 파셀에
// 숫자로 박으면 수면을 옮길 때마다 여기도 손으로 고쳐야 하고, 그것이 값 미러링이다.
// 두께 자체의 정당성은 `tests/world2-water.test.ts` 가 지킨다 — 골든이 볼 것은 **배치**다.
import { GROUND_DEPTH } from '../frontend/js/world2/parts/ground.js';

/** 부동소수 표기 흔들림을 막으려 자릿수를 고정한다 */
function ser(p: PlacedPart): string {
  const f = (n: number) => n.toFixed(6);
  return `${p.kind}|${f(p.x)}|${f(p.z)}|${f(p.y)}|${f(p.ry)}|${f(p.sx)}|${f(p.sy)}|${f(p.sz)}|${p.tone}`;
}

const TIERS = ['near', 'mid', 'far'] as const;

/** 25×25 격자 × 3 tier. 좁은 표본은 우연히 같을 수 있다 */
function gridDigest(): { hash: string; parts: number } {
  const lines: string[] = [];
  let parts = 0;
  for (let px = -12; px <= 12; px++) {
    for (let pz = -12; pz <= 12; pz++) {
      for (const t of TIERS) {
        const placed = parcelLayout(px, pz, t);
        parts += placed.length;
        lines.push(`${px},${pz},${t}:${placed.map(ser).join(';')}`);
      }
    }
  }
  return { hash: createHash('sha256').update(lines.join('\n')).digest('hex'), parts };
}

describe('배치 골든 — 리팩터가 세상을 바꾸지 않았다', () => {
  it('25×25 격자 × 3 tier 의 배치가 고정값과 일치한다', () => {
    const { hash, parts } = gridDigest();
    // 해시가 어긋나면 무엇이 달라졌는지 알 수 없으므로, 파츠 총수를 함께 본다 —
    // "개수가 같은데 해시만 다르다"와 "개수부터 다르다"는 원인이 전혀 다르다.
    // ── 2026-08-02: 37,146 → 37,124 (−22) ───────────────────────────────────
    // 미술관 GLB 가 기본 노출로 올라오며 **광장 서쪽 칸 하나**를 통째로 차지했고, 그
    // 칸에서 나무·벤치·화분이 빠졌다(`parts/plaza.ts` 의 `plazaOccupied`).
    //
    // 골든을 고치기 전에 **그 −22 가 정말 그 칸의 몫인지 실측했다**: 배제 후 그 칸의
    // 소품이 3 tier 합계 0 이고, 나머지 광장 8칸은 116 로 살아 있다. 골든이 잡은 차이와
    // 배제한 대상이 일치한다. (이 확인 없이 숫자만 바꾸면 골든은 "무엇이든 지금 값" 을
    // 옳다고 말하는 장식이 된다.)
    expect(parts).toBe(37124);
    expect(hash).toBe('bc4a31be1551fd0838092f6c0b2b1d900f219f9aa53fd153c6657f006a9ea0ad');
  });

  // 해시 불일치는 "어딘가 다르다"까지만 말해준다. 한 파셀을 통째로 박아 두면
  // 실패했을 때 **어느 필드가 어떻게** 달라졌는지 diff 로 바로 읽힌다.
  it('대표 파셀 (3,-7,near) 의 배치가 항목까지 일치한다', () => {
    // **사거리**(road 9조각 = 중심 + 네 방향 각 2). 격자화 전에는 북·동만 열린 꺾인 길이라
    // 5조각이었다. 건물 **3채가 각각 다른 사분면**에 서 있고 전부 `SETBACK` 밖이다 —
    // 겹침 방지와 인도 확보가 걸린 증거다. (예전에 여기 "≥ 7(SETBACK)" 이라 적었는데
    // 셋백이 유도값이 되면서 거짓이 됐다. 수치를 다시 적지 않는다 — 셋백을 실제로 지키는
    // 것은 `world2-road.test.ts` 의 전수 검사이고, 여기는 좌표 고정이 일이다.)
    //
    // 격자화에서 **도로 아래 항목의 좌표가 한 자리도 안 바뀌었다.** 도로 방향은 건물
    // 배치의 입력이라 함께 흔들릴 수도 있었는데, 사분면 1:1 배정이 방향과 무관해서
    // 그렇지 않았다. 변경이 의도한 범위에 머물렀다는 뜻이다.
    expect(parcelLayout(3, -7, 'near').map(ser)).toEqual([
      `ground|0.000000|0.000000|0.000000|0.000000|32.000000|${GROUND_DEPTH.toFixed(6)}|32.000000|1`,
      'garden|0.000000|0.000000|0.070000|0.000000|32.000000|1.000000|32.000000|2',
      // 조각 **수**는 9 그대로이고 폭(`sx`)이 8 → 5 로 좁아졌다. 중심 조각이 작아진
      // 만큼 팔이 길어져(6 → 6.75) 파셀 변까지는 여전히 정확히 닿는다 — 길이 파셀
      // 경계에서 끊기지 않는다는 계약이 도로 폭과 무관하게 유지된다는 뜻이다.
      'road|0.000000|0.000000|0.140000|0.000000|5.000000|1.000000|5.000000|0',
      'road|0.000000|-5.875000|0.140000|0.000000|5.000000|1.000000|6.750000|0',
      'road|0.000000|-12.625000|0.140000|0.000000|5.000000|1.000000|6.750000|0',
      'road|0.000000|5.875000|0.140000|0.000000|5.000000|1.000000|6.750000|0',
      'road|0.000000|12.625000|0.140000|0.000000|5.000000|1.000000|6.750000|0',
      'road|-5.875000|0.000000|0.140000|1.570796|5.000000|1.000000|6.750000|0',
      'road|-12.625000|0.000000|0.140000|1.570796|5.000000|1.000000|6.750000|0',
      'road|5.875000|0.000000|0.140000|1.570796|5.000000|1.000000|6.750000|0',
      'road|12.625000|0.000000|0.140000|1.570796|5.000000|1.000000|6.750000|0',
      // 세 채가 셋백이 물러난 만큼 **원점 쪽으로 밀렸다**. 크기·회전·톤은 소수점 여섯
      // 자리까지 그대로다 — 이번 변경이 배치 한계만 건드렸다는 증거다.
      'building|10.104035|-10.655234|0.000000|1.570796|4.605185|5.106515|4.737921|1',
      'building|-10.950478|-11.790029|0.000000|3.141593|5.299035|14.772182|5.681980|0',
      'building|11.616311|11.360009|0.000000|3.141593|5.849062|5.553167|5.547704|3',
      // 사거리인데 가로등이 넷뿐이다 — `east` 경계(x=16)와 `south` 경계(z=16)만 자기
      // 것이고, `west`·`north` 쪽 두 쌍은 이웃 파셀 (2,-7)·(3,-8) 이 자기 `+` 로 세운다.
      // 세계좌표로 이어 보면 빠지는 자리가 없다는 것은 `world2-lamp-placement.test.ts`
      // 의 경계 전수 검사가 본다.
      //
      // 인도 축 값이 6.25 → 4.5 다. 도로가 좁아졌으니 인도도 안쪽으로 따라온 것이고,
      // **경계 축(16)은 안 움직였다** — 그 자리는 셀 크기가 정하지 도로 폭이 정하지 않는다.
      'lamp|16.000000|4.500000|0.000000|0.000000|1.000000|1.000000|1.000000|0',
      'lamp|16.000000|-4.500000|0.000000|0.000000|1.000000|1.000000|1.000000|0',
      'lamp|4.500000|16.000000|0.000000|0.000000|1.000000|1.000000|1.000000|0',
      'lamp|-4.500000|16.000000|0.000000|0.000000|1.000000|1.000000|1.000000|0',
      // 셋백이 물러나 슬롯이 하나 열렸고 나무가 2 → 3 그루가 됐다. 앞 두 그루는
      // 크기·회전이 그대로이고 자리만 옮겼다.
      'tree|-8.058865|11.933178|0.000000|1.299119|1.100342|1.045542|1.100342|0',
      'tree|-11.281948|8.546820|0.000000|2.554736|0.756356|0.776562|0.756356|1',
      'tree|-7.875000|7.875000|0.000000|4.517040|1.604489|1.622547|1.604489|1',
      'planter|-11.861246|11.235764|0.000000|0.000000|1.064310|1.064310|1.064310|2',
    ]);
  });
});
