# OpenArtShow — 작업 지침 (Claude Code)

## ⚠️ 언어 — 반드시 한국어

- **사고과정(thinking/reasoning)을 한국어로 작성한다.** 감독이 사고 흐름을 한국어로 읽는다. 영어로 생각을 전개하지 말 것.
- 감독·팀 응답도 한국어로 작성한다.
- 커밋 메시지·개발일지(DEVLOG)·문서도 한국어.
- 예외: 코드·식별자·라이브러리명 등 원래 영어인 것은 그대로. (그 외 서술은 전부 한국어)

## 프로젝트 한 줄
브라우저 기반 3D 가상 미술관(three.js) — 작가가 대관료·액자·창고비 없이 자기 전시를 여는 "창(窓)".
GitHub Pages 정적 호스팅. "파라미터가 곧 공간/아바타" (무저장·기기별 렌더). 왜 만드는지는 `docs/FOUNDING.md`.

## 핵심 개발 규율
- **behind-flag**: 어디에도 링크하지 않는 페이지는 `visit.html`·`lab-glb.html`·`world2.html`이다(라이브 미노출). 플래그 제거·라이브 노출은 **감독·팀장 게이트**. — `builder.html`은 감독·팀장 게이트를 거쳐 이미 라이브다(`studio.html`이 "전시 공간 직접 꾸미기(베타)" 카드로 링크). 이 줄이 오래 `builder.html`을 behind-flag로 적고 있었고 스모크 대상에서도 빠져 있었다(회귀 게이트 구멍).
- **배포 게이트(§10)**: 구현 → **독립 executor 스모크**(구현자 아닌 별도 에이전트) → **검수관(release-reviewer) 교차리뷰** → 승인 후에만 push. **검수관 판정에는 구속력이 있다** — 반려는 병합 금지, 조건부 승인은 재확인 후 병합. PR 본문에 판정 기록이 필수이고 `review-record.yml` 이 검사한다. 전문·한계·우회 절차는 [.claude/agents/release-reviewer.md](.claude/agents/release-reviewer.md) 의 "반려의 구속력" **한 곳**이다 — 여기에 조항을 다시 적지 않는다(규율에서 값 미러링을 재현하지 않는다, 팀장 조건 2026-07-30). main push → `deploy.yml` 자동배포 → Actions success 확인. — 단 **`verify-live` job 의 빨간불은 "배포 실패"가 아니다**(그것은 publish 뒤에 도는 알림이다). "success 확인"은 `verify`→`deploy` 까지를 뜻한다. 판정·런북은 `ARCHITECTURE.md §7`.
- **publish 이후를 보는 축이 생겼다(#128).** `verify-live` 가 배포된 URL 을 실제로 열어 **HTTP 200 · 자산 실패 0 · 콘솔 에러 0 · 배포 판본 일치**를 본다. `guide.html` 라이브 404 계열과 `.nojekyll` 유실은 **이 축으로만** 잡힌다(로컬 스모크는 정적 서버라 `.nojekyll` 없이도 통과한다). **막지 않고 알린다** — 자동 롤백은 일부러 안 묶었다(롤백은 이 저장소에서 이미 사고를 낸 메커니즘이다). 승격 조건은 태스크 #128. 라이브에서 **못 잡는 것**: 자기완결(외부 요청이 성공해버려 안 잡힌다 — `smoke:vite` 소관), 렌더 결과의 유의미성(200·0·0 은 빈 화면과 구별 안 된다), 시각 회귀, WebGPU.
- **CI 게이트의 현재 위치 — 배포를 막는다(2026-07-29 결선 완료, #127).** `deploy.yml` 의 `verify` job(`uses: ./.github/workflows/ci.yml`)을 `deploy: needs: verify` 로 잇는다. verify 가 떨어지면 deploy job 이 **아예 뜨지 않는다** — 실측 근거는 run `30434091542`(정상 → deploy 실행)와 `30434607110`(타입 에러 → deploy **skipped·0초**)다. `ci.yml` push 트리거는 `claude/**` + PR + `workflow_call`(main 은 뺐다 — 남기면 이중 실행).
  **그러나 "CI 통과 = 라이브 정상"이 아니다. 남은 한계 셋을 함께 읽는다:**
  ① **publish 이후 라이브 URL 검증이 0이다.** 게이트는 전부 `_site` 조립본까지만 본다. `guide.html` 라이브 404 계열 사고는 **이 게이트로 안 막힌다**(검수관을 신설한 바로 그 사고다). ② **`main` 은 unprotected 다** — 게이트는 워크플로를 경유하는 정직한 경로만 막는다. `ci.yml` 을 비우는 커밋은 자기 자신의 빈 게이트로 통과하고, gh-pages 직접 push 도 막을 것이 없다. ③ **`[7][8]` 은 observe 중이라 성능 축은 아직 아무것도 안 막는다**(콘솔 에러·pageerror 축은 막는다).
  조립 레시피(`scripts/smoke/assemble.mjs` 의 `assembleSiteVite`)는 `deploy.yml` 과 1:1 미러링이므로 **한쪽을 고치면 반드시 다른 쪽도 본다**(어긋나면 "스모크 PASS + 배포 실패"가 성립한다 — 실제로 성립해 있었다). 배포 게이트가 도는 스모크는 **`smoke:vite` 하나다**(`npm run smoke` 는 baseline = 대조군 전용).
- **성능은 스모크가 지킨다**(감독 지시 2026-07-29 *"개발할때 성능 확보, 성능 유지하는게 모니터링하도록 해"*). `smoke:vite`에 두 게이트가 묶여 있다 — **[7] 개수 불변식**(회전·주행·재방문 세션 시뮬)과 **[8] 하늘 예열**(날씨 첫 등장). world1 CSV가 확정한 것을 지킨다: 파이프라인 60→227 단조증가가 프리즈의 직접 원인이었고 world2는 상수다. **회전을 반드시 돈다** — 아무것도 로드하지 않고 카메라만 돌려도 파이프라인이 늘어 fps가 60→10이 됐다(스트리밍과 독립된 증식 축). 프레임 시간은 **안 잰다**(swiftshader는 실기기와 무관 — 오늘도 헤드리스 +21%가 실기기 0이었다). FAIL이면 `npm run measure:invariants` / `measure:sky-warm`으로 구간별 표를 본다. **한계**: `info.memory`는 첫 렌더에 오르므로 화면에 안 나오는 누수는 못 잡는다(뮤테이션으로 확인 — 화면 밖 지오 생성은 PASS로 통과했다). CI 러너에서는 `SMOKE_PERF_GATES=observe`로 **성능 판정만** 관측한다(INFO로 적어 종료코드에서 뺀다) — 러너 성능 편차의 거짓 FAIL 위험을 아직 안 재봤기 때문이다. **콘솔 에러·pageerror가 잡히면 observe에서도 FAIL이다** — 페이지 에러는 성능 편차가 아니고, 조작 중에만 나는 에러를 보는 축은 `[7][8]`뿐이다(`[4]`는 로드 시점만 본다). 승격 조건의 SSOT는 `scripts/smoke/run.mjs`의 `PERF_GATES` 주석이고 회차 집계는 태스크 #126이다 — **여기에 값을 다시 적지 않는다**(한쪽만 고쳐도 아무도 모른다). **관측은 면제가 아니라 데이터 수집이다** — 승격 없이 남으면 그때부터 장식이다.
- **라이브 런타임 보호**: `frontend/js/main.js`·`player.js`·`artworks.js`·`config.js`는 함부로 수정하지 않는다(고정 미술관 서비스 중). 방문자뷰는 가산형 독립 모듈(`visit.js`).
- **자기완결**: 외부 호스트 0(CDN·폰트·이미지·GLB 금지). CSP `default-src 'self'`. three는 `/vendor/three.module.js`.
- **IP**: 특정 게임/브랜드 트레이드드레스·실존 상호·인물 금지. 파츠 에셋은 자작 지오메트리만(외부 에셋은 §6 법무).
- **커밋**: `git config user.email noreply@anthropic.com` / `user.name Claude`. push는 `-u origin <branch>` + 실패 시 지수백오프 재시도. **모델 식별자를 커밋·PR·코드·아티팩트에 넣지 않는다**(채팅 한정).
- **게이트는 `npm run gate` 하나다 — 손으로 조립하지 않는다** (감독 지시 2026-07-30 *"자꾸 빠뜨리고 실패하고"*). `git add` 를 마친 뒤 돌리고, 통과하면 index 트리 해시가 `.gate-stamp` 에 남고 `scripts/githooks/pre-commit` 이 그것을 대조해 **불일치·미실행이면 커밋을 막는다**(실측: 게이트 실패 시·게이트 후 파일 변경 시·스탬프 없을 때 3케이스 전부 차단). 훅 설치는 `.claude/settings.json` 의 SessionStart 가 `core.hooksPath` 로 한다.
  **왜 구조로 옮겼나** — 같은 실패를 두 번 했고 원인이 같았다. ① `;` 로 나열해 typecheck=2 를 화면에서 보고도 커밋 ② `set -e` 로 고쳤으나 `| tail` 로 파이프해 **pipefail 부재로 종료코드가 삼켜져** 또 통과로 적었다. 형태만 달랐고 **매번 손으로 조립하니 매번 다르게 틀렸다.** 조립을 없애면 틀릴 자리가 없다. 스모크는 넣지 않는다(§10-3 독립 executor 소관).
- **위임 프롬프트는 [docs/DELEGATION.md](docs/DELEGATION.md) 의 블록을 복사해서 쓴다.** 손으로 조항을 쓰면 빠진다 — executor 에게 *"테스트를 느슨하게 만들지 말 것"* 을 안 적어서 실제 산술 단언이 전부 null 기대로 바뀐 채 CI 를 통과했고, 검수관 반려(블로커 4건) → 재작업이 났다. 조항 한 줄이 없어서다.
- **스크래치**: 임시 파일은 세션 스크래치패드에. 헤드리스 QA 하네스는 `/opt/pw-browsers` 크로미움 + swiftshader. **작업 종료 시 스크래치 임시 파일(스크립트·스크린샷·로그)은 즉시 정리한다**(매 턴 재주입·컨텍스트 누적 방지).

## 토큰·컨텍스트 절약 (부팀장 운영 규율)
- **위임 트리거 — 형용사가 아니라 조건문이다 (감독 지시 2026-07-29).** 아래 중 **하나라도 해당하면 위임한다.** 판단이 필요한지 스스로 묻지 않는다 — 그 물음이 새는 자리다.
  - 🍃 **executor**: ① 같은 형태의 편집을 **파일 3개 이상**에 반복 ② 헤드리스 QA·스크린샷 스윕 ③ 생성기 재빌드 ④ **뮤테이션 실행**(설계는 내가, 실행은 위임) ⑤ 배포 전 스모크(§10-3 — **구현자 본인 금지**) ⑥ 대용량 조회·로그 파싱
  - 🎨 **디자이너**: 색·간격·타이포·프레이밍·룩의 판단, 시각 회귀 육안 판정, CSS 구조 설계
  - 🔎 **검수관**: `.github/workflows/**`·`scripts/smoke/**` diff (§10-4 — 무조건)
  - 🧠 **팀장**: 설계 분기 — 선택지가 둘 이상이고 되돌리기 비싼 것 ② **검수관 자신에 관한 diff**(직급·급여·구속력 조항·`release-reviewer.md`) — 검수관이 자기 권한을 판정하면 제3자 검증이 아니다 ③ **감독에게 물었는데 답이 없는 결정**(감독 지시 2026-07-31 *"질문했을때. 내가 대답이 없으면 팀장이 결정하고 보고해."*)
    - **"답이 없다" 의 판정 기준**: 내가 질문을 올린 뒤 **감독의 다음 발화가 그 질문에 답하지 않으면** 답이 없는 것이다. 그 시점에 팀장에게 올린다 — 몇 시간을 기다린다든지 하는 시간 기준이 아니다(그러면 또 "언제까지?" 가 매번 판단거리가 된다).
    - **묻고 멈춰 서 있지 않는다.** 이 조항이 생긴 이유가 그것이다 — 감독이 답할 때까지 작업이 서면 감독이 병목이 된다. 팀장이 결정하고 **나는 그 판정을 감독께 보고한다**(결정을 숨기지 않는다).
    - 감독이 나중에 다르게 판단하면 그것이 최종이다. 팀장 판정은 **공백을 메우는 것**이지 감독을 대체하는 것이 아니다.
  - 🧠 **팀장 조건의 수단을 바꿀 때** (팀장 지침 2026-07-31): *"감독 직접 지시가 팀장 조건의 **수단**과 충돌하면 부팀장이 수단을 교체할 수 있다. 단 **세 요건 전부**: ① 조건의 **목적**을 게이트/테스트로 검증 가능하게 보존 ② 검수관 교차확인 ③ 같은 배포 사이클 내 팀장 **사후 통지**. 하나라도 못 채우면 사전 에스컬레이션이다."*
    - **감독 지시가 없으면 가르지 마라.** 팀장 조건을 목적/수단으로 갈라 수단을 바꾸는 것은 그 자체가 설계 분기이고 팀장 몫이다 — 그때는 사후 통지가 아니라 **사전 판정**을 받는다. 이번에 성립한 것은 내 해석이 옳아서가 아니라 **상위 근거(감독)가 이미 방향을 정했고 나는 집행 방식만 골랐기** 때문이다.
    - ③의 통지는 **실증 근거 + 목적을 보존한 수단 + 검수관 판정**을 갖춰야 한다. *"바꿨다"* 만 적은 통지는 통지로 치지 않는다(팀장 명시).
    - 첫 적용: 조건 4(`emissive` 금지) → 감독 *"흰 반짝임효과 해줘"* + 기존 수단이 실기기에서 0 이라는 실증 → 수단 교체, 목적(밤에 물이 밝아지지 않을 것)은 `waterGloss(time).sparkle` 시간대 분기 + 검은 바탕 발광맵 두 겹으로 보존. **추인됨.**
  - ⚙️ **부팀장(나)**: 분해·명세·통합·게이트. **그리고 그것만.**

  왜 조건문으로 다시 쓰는가 — **2026-07-29 실측.** 그날 검수관(2회)과 팀장(1회)은 불렀는데 executor 는 **0회**였다. 차이는 규율의 무게가 아니라 **트리거의 형태**였다: 검수관은 "diff 가 이 경로면 무조건" 이라는 조건문이었고, executor 는 *"반복 정형 작업"* 이라는 형용사였다. 형용사는 매 순간 "이건 판단이 좀 들어가니까 내가" 로 빠져나갈 수 있고, 실제로 CSS 3개 파일 읽기와 뮤테이션 14케이스를 손으로 했다. 감독이 *"낮은 팀원이 하고 있는거지?"* 라고 물어서야 드러났다.
  **어겨도 아무 일이 안 일어나는 것이 문제의 핵이다** — 코드 결함은 게이트가 잡지만 운영 규율에는 게이트가 없다. 그래서 최소한 트리거만이라도 셀 수 있게 만든다. 태스크에는 **착수 시 담당을 적는다**(비어 있으면 그 자체가 신호다).

  서브에이전트는 **결론만 텍스트로** 반환하므로 스크린샷·로그가 메인 컨텍스트에 쌓이지 않는다.
- **위임 직전에 현황판을 켠다** — `Monitor` 로 `node scripts/watch-agents.mjs`. 직원이 지금 무엇을 하는지, 멈췄는지, **저장소를 고치고 있는지(🔴)** 가 한 줄로 올라온다. 왜 필요한가: 검수관 위임 후 결과가 오지 않았고 "실행 중" 으로 믿은 채 **8시간 50분**을 잃었다(2026-08-04). 실제로는 최종 판정을 다 쓰고 정상 종료했는데 **결과 반환만 실패**한 것이었고, 완료 알림이 안 오는 상태는 "아직 도는 중" 과 구별되지 않는다. 생사는 `tasks/<id>.output` 으로 판정하지 않는다 — 그것은 심볼릭 링크라 mtime 이 생성 시각에 고정된다(진행은 링크 대상 JSONL 에 있다). **상시로 켜두지 않는 이유**: `Monitor` 는 30분이 상한이고, 재무장을 잊으면 감시가 조용히 사라져 "아무 일 없음" 과 구별되지 않는다 — 사고와 같은 형태다. 켜는 시점을 위임에 묶으면 잊을 자리가 없다.
- **스크린샷을 메인에서 직접 열지 않는다**: 이미지는 토큰이 크다. 육안 판정은 위임하고 결론만 받는다.
- **검증은 한 발에**: 헤드리스 환경 한계(swiftshader ~4fps, `preserveDrawingBuffer:false`, 비네트 오버레이)를 전제로 맞는 방법(예: 채도 분리)으로 바로 간다. 무효 검증 시행착오 금지.
- **미세 조정은 배치**: 파라미터 후보(예: 그림자 radius)는 스윕으로 한 번에 비교해 **최종값 1커밋/1PR·게이트 1회**. 조금씩 여러 PR로 쪼개 게이트를 반복하지 않는다.
- **거대 MCP 결과 회피**: `actions_list`처럼 수십만 자를 뱉는 조회는 단일 `run_id` 조회로 좁히거나 executor에 위임한다. 파싱은 Bash/python으로 컨텍스트 밖에서(`minimal_output`·`per_page` 최소화).
- **브랜치 위생 선제**: 작업 *착수 전* `git fetch origin && git merge origin/main`으로 최신 정렬부터 한다. 낡은 base로 진행하면 라이브 회귀가 섞여 교차리뷰 반려→재게이트(스모크·리뷰·병합검증 전면 재실행)로 토큰이 배로 나간다. 정렬은 착수의 첫 스텝.
- **생성물은 추적하지 않는다**(2026-07-27 구조화로 해소). `devlog/*.html`·`team/`·`valuation/`·`sitemap.xml`·`robots.txt`는 `scripts/build-*.mjs`가 만들고 CI가 배포 시 생성한다 — `.gitignore` 대상이다. 예전에는 이것들이 커밋돼 있어 개발일지를 한 줄 고칠 때마다 산출물이 워킹트리에 쌓였고, 그게 게이트 중 uncommitted 경고로 누적돼 정작 봐야 할 변경을 가렸다(별도 커밋으로 우회하던 것을 구조로 없앴다). **단 `devlog/img/`는 손으로 넣은 스크린샷이라 추적한다** — 생성 디렉터리라고 통째로 무시하면 자산이 날아간다(실제로 한 번 날렸다).

## 검증 규율 — 같은 실수를 반복하지 않기 위한 것 (감독 지시 2026-07-27)

전부 **실제로 일어난 사고**에서 나왔다. 문서에만 있고 게이트가 아니면 또 반복된다 — 검사할 수 있는 것은 검사로 만든다.

- **못 잰 것은 통과가 아니다.** 측정이 실패했으면 "무엇이 막았는지"를 쓴다. 추측으로 메우거나, 재지 못한 항목을 조용히 생략하지 않는다. — 실제 사고: 스모크가 "로딩 0.1초 완료"로 보고했으나 실측 1,418ms였고, "script error 0건으로 개수 안정성 간접 입증"이라는 성립하지 않는 논리가 통과로 적혔다. **"못 잰 것이 통과로 적히는 경향"** 이 이 프로젝트의 상시 위험이다.
- **테스트 통과는 검출력의 증거가 아니다.** 결함을 고쳤으면 **뮤테이션으로 확인한다** — 그 결함을 일부러 되살려 테스트가 실제로 깨지는지 본다. 안 깨지면 게이트가 아니라 장식이다. — 실제 사고: 구름 `alpha` 미소비를 고치고 "값이 무시되면 깨지는 테스트를 넣었다"고 적었으나, 순수 함수 안에서만 참이었고 정작 버그가 있던 통합 지점은 아무 테스트도 안 봤다(검수관이 잡음).
- **판정/집행 분리의 구멍 — 경계를 건너는 지점은 아무도 안 본다.** `decide/`를 순수 함수로 두면 각 쪽은 테스트하기 쉬워지지만, "계산된 값이 실제로 소비되는가"는 양쪽 테스트 어디에도 안 걸린다. 새 판정 값을 만들면 **집행 쪽 통합 테스트를 함께** 붙인다(three 의존이면 스텁으로 대체해 실제 코드를 돌린다 — `tests/world2-sky-system.test.ts`가 본보기).
- **에이전트는 워킹트리를 바꾸지 않는다.** 대조군 비교가 필요하면 별도 worktree/클론을 쓴다. `git checkout <commit> -- <path>`로 되돌린 뒤 복구하지 않으면 **이후 모든 측정이 옛 코드를 잰 것**이 되고, 그 사실이 보고서에 안 나타난다. 위임 프롬프트에 "저장소 파일 무수정 + 최종 `git status --short` 보고"를 반드시 넣고, 받은 보고에서 그 출력을 확인한다. — 실제 사고 2건: (a) 재게이트 스모크 중 sky 두 파일이 이전 커밋 상태로 되돌아간 채 staged로 남았다. (b) executor가 **미커밋 신규 테스트 파일을 삭제**했다 — "스크래치 임시 파일 정리" 지시를 저장소까지 확대 적용했고, 프롬프트에 무수정 조항이 있었는데도 났다.
- **`git status`가 clean이라는 보고는 무결성의 증거가 아니다.** 지워서 clean일 수도 있다. 위임 전 워킹트리에 **미커밋 산출물이 있으면 먼저 커밋**하고 보내라 — 커밋되지 않은 것은 복구 수단이 없다. 받은 보고의 clean은 "내가 아는 미커밋 파일이 그대로 있는가"를 직접 확인한 뒤에만 통과다.
- **헤드리스는 WebGL(swiftshader)이고 감독 실기기는 WebGPU다.** 두 백엔드는 **렌더 경로가 다르다** — `ShaderMaterial`(GLSL)은 `three.webgpu` 빌드에 렌더 경로가 아예 없다. 헤드리스 통과를 WebGPU 근거로 쓰지 않는다. 백엔드 의존 API를 쓸 때는 `adapters/renderer.ts`에 가두거나, 두 백엔드에서 동일한 수단(캔버스 텍스처 등)을 고른다. **이 사각은 아직 열려 있다** — 헤드리스로 WebGPU를 검증할 방법이 없으므로 감독 실기기 확인이 유일한 판정이다.
- **값 미러링은 색·수치·테스트 임계값에도 적용된다.** 같은 값을 두 곳에 적으면 한쪽만 고쳐도 아무도 모른다. — 실제 사고 3건: 캔버스 `rgba(36,31,28,...)`가 `HAZE` 상수와 따로 놀아 상수를 바꿔도 반영 안 됨 / 구름 고도 `160`·`260`이 배치와 색 보간에 각각 하드코딩 / 테스트가 옛 `minY`(120) 기준 임계값을 박아둬서 `minY`가 바뀌자 표본이 빈 배열이 되고 **빈 평균 0이 단언을 통과**시킴.
- **두 버그가 서로를 상쇄하고 있을 때, 하나만 고치는 것은 고치기 전보다 나쁠 수 있다.** — 실제 사고 2건: (a) 색공간 오류가 하늘을 3배 밝혀 팔레트 대비 부족(3.5%)을 가리고 있었다. 한쪽만 고치면 하늘이 도로 안 보이거나 한낮 하늘이 된다. (b) 슬롯 예산의 `MAX_PARCELS=20`이 이론 최악치(21)보다 작았는데, 곱해둔 여유 배수 1.25가 그 부족을 덮어 `starved`를 계속 0으로 보이게 했다 — **지표가 정상인 것이 두 값 다 맞다는 뜻은 아니다.** 증상이 하나여도 원인이 하나라고 가정하지 않는다.
- **참인 문장에서 성립하지 않는 결론을 뽑는 것 — 값이 아니라 재는 축이 틀린다.** 개수 불변식을 "객체를 부팅 때 다 만들어 둔다"로 보증했는데, three의 `info.memory`는 객체 생성이 아니라 **첫 렌더**에 오른다. 재워둔(`visible=false`) 메시는 렌더 목록에 안 올라 GPU 자원이 아예 없다 — **만들어 둔 것과 GPU에 올라간 것은 다른 일이다.** 그래서 날씨를 처음 켜는 순간 계단이 났다(실기기 pipeline 31→33·geometry 92→95·texture 32→35). 앞 문장은 내내 참이었다. 주석이 보증하던 것은 게이트로 옮긴다(`npm run measure:sky-warm`).
- **"실측에 여유를 얹은 값"은 근거가 아니다.** 실측은 밟아본 경로의 최댓값일 뿐 도달 가능한 최댓값이 아니다. 유도할 수 있는 값이면 유도한다 — 그러면 전제(밴드·레이아웃)를 바꿔도 저절로 따라오고, 실측을 다시 뜰 필요도 없다. — 실제 사고: 위 (b)의 20이 "헤드리스 30샘플 최대 17에 여유를 얹은" 값이었고, 밴드에서 유도한 이론 최악치는 21이었다.

## 코드 아키텍처
- **SSOT는 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** — SOLID 원칙·강제 불변식(보호파일 import 0·순환 0·SSOT 경유)·배럴 패턴·모듈 지도·배포 파이프라인. 새 기능 착수 전 먼저 읽는다.

## 공간 빌더 스키마
- `frontend/js/space.ts`(소스, `.js`는 산출) = SSOT("파라미터가 곧 공간"). 버전 필드 + `normalizeSpace`/`migrateSpace`(하위호환). 저장은 localStorage/JSON만(URL 인코딩 금지).
- 렌더 조립: `frontend/js/space-render.js`는 얇은 **배럴**(재수출)이고, 실제 정의는 `space-parts.ts`(`partGeo`/`partAccent`/`MATS`)·`space-assembler.ts`(`buildSpaceGroup`·`addRoomLighting`)·`space-lightmap.js`(`bakeShellLightmaps(Async)`)에 있다. 소비자는 배럴 경로로 접근.
- 파츠 지오메트리 교체는 `space-parts.ts`의 `partGeo`/`partAccent`/`MATS` 3지점만 손대면 스키마·빌더 로직 무영향.
