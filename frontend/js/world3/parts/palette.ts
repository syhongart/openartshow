// 포근마을 팔레트 — **색의 유일한 출처.**
//
// ── 왜 world3 에는 이 파일이 있고 world2 에는 없었나 ────────────────────────
// world2 의 색은 파츠 13개 파일에 **47곳 하드코딩**돼 있었다. 그래서 "테마만 바꾼다"
// 가 불가능했고, 그 사실이 이 포크의 착수 근거가 됐다(`../README.md`).
//
// 포크하면서 같은 구조를 그대로 복제하면 다음 사람이 또 같은 벽을 만난다. 마을 톤을
// 한 번 손보려고 파일 13개를 열게 만드는 것이 그 벽이었다. **색을 여기 모으는 것이
// 이번 개조에서 실제로 값을 하는 부분이다** — 지오메트리를 둥글게 만드는 것보다.
//
// ── 규약: 파츠는 색 리터럴을 쓰지 않는다 ────────────────────────────────────
// `tests/world3-palette.test.ts` 가 `parts/` 안의 hex 리터럴을 세어 이 규약을 지킨다.
// 색이 필요하면 여기에 **이름을 붙여** 추가한다. 이름이 곧 그 색의 역할이고, 역할이
// 없는 색은 대개 다른 색으로 대체할 수 있다.
//
// ── `tones` 와의 관계 — 이 값들은 곱셈기가 아니다 ───────────────────────────
// 여기 있는 색은 **정점에 굽는 색**이다(`bake.ts` 의 `rgb()` 를 거친다). 파츠의
// `tones` 는 그것에 **곱해지는** 인스턴스 틴트라 별개이고, 흰색 근처여야 한다
// (`TINTS` 참고). 둘을 섞어 쓰면 나무가 초록×초록으로 새까맣게 죽는다 —
// world2 에서 실제로 겪었고 `tests/world2-parts-assets.test.ts` 에 남아 있다.

/**
 * 마을 본색. **정점색으로 굽는 값**이다.
 *
 * 채도가 높은 편인데 의도다 — 감독 지시가 *"귀욤 뽕짝"* 이었고, 낮은 채도는 차분해질
 * 뿐 귀여워지지 않는다. 다만 **명도는 대체로 높게** 잡았다: 어두운 색이 섞이면
 * 파스텔 통일감이 깨지고 화면이 탁해진다.
 *
 * ── ⚠️ 이 중 12색이 world2 에 **복사돼 있다** (2026-08-09) ──────────────────
 * 감독 지시(*"시계탑도 동물의 숲에 있는 시계탑을 world2로 교체하자"*)로 종탑을 옮기며
 * `stone`·`wall`·`wallBase`·`door`·`glass`·`trim`·`roof`·`roofShade`·`brass`·`metal`·
 * `wood`·`woodDeep` 가 `world2/parts/clocktower.ts` 로 복사됐었다.
 *
 * ⚠️ **그리고 같은 날 지워졌다** — 감독이 두 안(`?clock=village|city`) 중 **`city`** 를
 * 골랐고, world3 값을 그대로 쓰던 `village` 팔레트는 제거됐다. 지금 world2 시계탑은
 * **도시 톤 자체 팔레트**를 쓰고 이 파일과 공유하는 값이 **하나도 없다.**
 *
 * 그러면 왜 이 문단을 남기나 — **되살릴 때의 출처가 여기이기 때문**이다. world2 쪽
 * 주석이 *"되살리고 싶으면 `world3/parts/palette.ts` 의 `V` 에 원본이 있다"* 로 이쪽을
 * 가리키고 있다. 양방향 중 한쪽만 남기면 그 링크가 끊긴다.
 *
 * (이 문단은 원래 *"복사본이 저기 있다"* 는 역참조였고 검수관 권고로 생겼는데, 하루도
 * 안 돼 그 복사본이 사라져 **문장이 거짓이 됐다.** 검수관이 다음 리뷰에서 잡았다 —
 * 참조를 적으면 참조 대상이 사라질 때 함께 고쳐야 한다는 사례로 남긴다.)
 */
export const V = {
  // ── 지면 ────────────────────────────────────────────────────────────────
  /** 잔디 본색. 마을 화면의 절반 이상을 차지하므로 여기가 톤 전체를 좌우한다 */
  grass: 0x86cf63,
  /** 잔디 변주(밝은 쪽) — 패치·정원에 섞어 단조로움을 깬다 */
  grassLight: 0x9bdd77,
  /** 잔디 변주(짙은 쪽) — 나무 그늘 아래·경계 */
  grassDeep: 0x63b348,
  /**
   * 흙길 바탕. **짙다** — 그게 이 값의 요점이다.
   *
   * ⚠️ **처음 값(`0xd8b585`, 베이지)은 틀렸고, 그것을 고치려던 첫 시도(`0xe8cfa4`,
   * 더 밝은 베이지)도 틀렸다.** 두 번 다 같은 착각이었다: 눈으로는 모래빛과 초록이
   * 확연히 다르니 대비가 있을 것이라고 본 것이다. 실측은 반대였다 —
   *
   *     luma(0xd8b585) = 0.7254   vs   luma(V.grass) = 0.7203   →   차이 **0.005**
   *
   * 초록은 휘도 계수가 커서(0.7152) 눈에 보이는 것보다 훨씬 밝게 계산된다. world2 가
   * *"지면 19% vs 도로 17%, 2%p 차라 통짜 검은 바닥"* 으로 값을 치른 그 함정에
   * **부호만 반대로** 빠진 것이다.
   *
   * 밝은 쪽으로 더 밀어서 0.15 를 넘기려면 백사장(`V.sand`)까지 가야 하고, 거기엔
   * 두 번째 대가가 있다 — 지면 파츠가 밝아지면 `decide/ground-albedo.ts` 의
   * `MAX_LIFT`(= 1/최대 지면 알베도)가 눌려 **감독이 화면에서 고른
   * `NIGHT_GROUND_LIFT = 2.4` 가 상한에 걸린다.** 룩을 고치려다 밤 판정을 무효로
   * 만드는 셈이다.
   *
   * 그래서 **어두운 쪽으로 갔다.** 짙은 흙 바탕 위에 마른 결(`dirtLight`)과 자갈을
   * 얹으면 길로 읽히고, 잔디와의 명도차는 0.23 으로 벌어지며, 선형 휘도가 잔디보다
   * 낮아 `MAX_LIFT` 축도 안 건드린다. 세 가지가 한 번에 풀린다.
   *
   * 실측 (`luma`): 바탕 **0.5055** · 잔디 0.7203 → 차 **0.2148**. 길 위에 뿌리는
   * 밝은 결(마른 결·잔모래·자갈)이 화면 평균을 0.5463 까지 올리므로 **실제로 눈에
   * 보이는 대비는 0.174** 다 — 규약 0.15 에 여유 0.024. 덧그림이 규약을 몰래 갉아
   * 먹는다는 것까지 계산에 넣은 값이고, 그 산식은 `road.ts` 의 텍스처 주석에 있다.
   *
   * 이 값을 다시 밝히려는 사람에게: **`luma()` 로 재 보고 오라.** 눈으로 고르면 또
   * 틀린다 — 두 번 틀렸다.
   */
  dirt: 0xa87a52,
  /** 흙길 위 마른 결. 짙은 바탕(`dirt`) 위에 얹혀 길의 결을 만든다 */
  dirtLight: 0xd8b585,
  /** 포장 돌 — 광장 바닥 */
  stone: 0xe0d8c8,
  /** 포장 돌 사이 줄눈 */
  stoneSeam: 0xc4b9a6,
  /** 모래(물가) */
  sand: 0xf0dfb4,

  // ── 건축 ────────────────────────────────────────────────────────────────
  /** 벽 크림. 마을 집의 기본 벽색 */
  wall: 0xfff2df,
  /** 벽 아랫단(굽) — 벽과 땅 사이를 끊어 준다 */
  wallBase: 0xe8d5bb,
  /** 지붕 본색(테라코타). 마을에서 가장 눈에 띄는 색이라 여기가 인상을 정한다 */
  roof: 0xe8705f,
  /** 지붕 그늘쪽 — 같은 색의 어두운 판이 아니라 살짝 붉게 남긴다 */
  roofShade: 0xc25a4c,
  /** 문 */
  door: 0xa9714a,
  /** 창틀·차양 */
  trim: 0xfffaf0,
  /** 창유리 — 낮에는 하늘을 비추는 셈이라 푸르게 */
  glass: 0xa8d8ea,
  /** 굴뚝 벽돌 */
  brick: 0xcf8a6e,

  // ── 초목 ────────────────────────────────────────────────────────────────
  /** 나무 줄기 */
  trunk: 0xa87551,
  /** 수관 아래단(가장 짙다) */
  leafDeep: 0x4fa249,
  /** 수관 중간단 */
  leaf: 0x66bd57,
  /** 수관 윗단(빛 받는 쪽) */
  leafLight: 0x8ad46b,
  /** 열매 */
  fruit: 0xf25c54,
  /** 덤불 */
  bush: 0x5fb35a,

  // ── 꽃 ──────────────────────────────────────────────────────────────────
  flowerRed: 0xf26d7d,
  flowerYellow: 0xffd166,
  flowerWhite: 0xfff6ea,
  flowerBlue: 0x8fb8f0,
  flowerPink: 0xf9a8d4,

  // ── 소품 ────────────────────────────────────────────────────────────────
  /** 목재(벤치·다리·표지판) */
  wood: 0xc99863,
  /** 목재 짙은 쪽 */
  woodDeep: 0xa77b4c,
  /** 금속(가로등 기둥·손잡이). 검정 대신 **따뜻한 짙은 갈색** — 순수 무채색은 파스텔
   *  화면에서 구멍처럼 보인다 */
  metal: 0x6b5544,
  /** 놋쇠 — 종·풍향구슬. 꽃 노랑(`flowerYellow`)을 전용하고 있던 것을 분리했다.
   *  역할이 다른 색을 공유하면 꽃 색을 바꿀 때 종이 함께 바뀐다(랜드마크 담당 지적) */
  brass: 0xe0a94f,
  /** 등불 유리(낮의 색) */
  lantern: 0xffe9a8,
  /**
   * 등불이 **밤에 내는 빛**. `lamp.ts` 의 `emissive` 다.
   *
   * ⚠️ **이 값을 바꾸면 `decide/night.ts` 의 `LAMP_LUMINANCE` 가 함께 움직인다.**
   * 저쪽은 이 색의 휘도를 블룸 문턱과 견주고, 그 비교가 밤 가로등이 번질지를 정한다.
   * 예전에 문턱 0.85 vs 휘도 0.805 로 **블룸이 정상 동작하면서 걸리는 픽셀이 0** 인
   * 상태가 있었고, 감독 화면에는 *"가로등 똑같은데"* 로만 나타났다 — 코드는 멀쩡한데
   * 결과가 없는, 원인을 짚기 가장 어려운 형태다.
   *
   * 그래서 저쪽이 **여기서 유도한다**(`luma(V.lanternGlow)`). 원래는 채널값
   * (`0xff`/`0xc8`/`0x6e`)을 손으로 베껴 적고 있었고 그것이 미러링이었다 —
   * 소품 담당이 잡았다(2026-08-08).
   */
  lanternGlow: 0xffc86e,
  /** 물 — 얕고 맑게 */
  water: 0x7fd0e0,
  /** 물 깊은 쪽 */
  waterDeep: 0x4fa8c4,
  /** 천막·차양 줄무늬 */
  awning: 0xf28b82,
} as const;

/**
 * 인스턴스 틴트. **`tones` 에 쓰는 값이고 곱셈기다.**
 *
 * ⚠️ 각 채널이 `0xd0`(82%) 이상이어야 한다 — 그 아래로 내려가면 정점색과 곱해져
 * 파츠가 어두워지고, world2 가 도로에서 실제로 그 사고를 냈다(최종 알베도 선형
 * 0.0003, 화면에서 길이 사실상 검정). `tests/world3-parts-assets.test.ts` 가 지킨다.
 *
 * 여기 값들은 **아주 옅은 색조 편차**다. 집집마다 완전히 다른 지붕색을 주지는 못한다 —
 * 곱셈은 어둡게만 만들 수 있고 밝게는 못 만들기 때문이다. 대신 마을 전체가 한 팔레트
 * 안에서 미묘하게 흔들리는 쪽을 골랐다. **그게 오히려 이 룩에 맞다**: 색이 제각각인
 * 마을은 번잡해 보이지 실제로 귀엽지 않다.
 */
export const TINTS = {
  /** 색 편차 없이 원색 그대로 */
  plain: 0xffffff,
  blush: 0xffe8e0,
  sky: 0xe0edff,
  butter: 0xfff6e0,
  mint: 0xe4f6e4,
  lilac: 0xf0e8ff,
} as const;

/** 파츠가 그대로 쓰는 기본 틴트 5종. 집·소품의 개체차를 만든다 */
export const TINT_SET: readonly number[] = [
  TINTS.plain, TINTS.blush, TINTS.sky, TINTS.butter, TINTS.mint,
];

/** 감마를 무시한 상대 명도. 대비 판정에는 이 근사로 충분하다 */
export function luma(hex: number): number {
  const r = (hex >> 16) & 0xff;
  const g = (hex >> 8) & 0xff;
  const b = hex & 0xff;
  return (0.2126 * r + 0.7152 * g + 0.0722 * b) / 255;
}
