// world10/decide/art-material.ts — **그림이 주변 환경을 얼마나 받는가.** 순수 (W8-7).
//
// ── 🔴 감독 지시 (2026-08-18) ─────────────────────────────────────────────
// *"그늘에 있으면 사진이 어두워. **사진은 주변환경에 영향을 안받았으면**"*
//
// 태스크 #87(「조명 못 받은 작품이 화면에서 눈에 띄게 어둡다」)이 감독 판정 대기였고
// 이것이 그 판정이다. **"사진은"** 이라고 특정하셨다 — 액자 테두리가 아니라 그림이다.
//
// 직접 원인은 그림 평면이 `MeshStandardMaterial` 이었던 것이다. 그 재질은 조명·그늘·
// 시간대를 그대로 받으므로, 그늘진 벽에 건 사진이 실제로 어두워진다.
//
// ── 🧠 팀장 판정 (2026-08-18) — (다) 채택 ────────────────────────────────
// > *"「주변환경」에는 **시간대 노출·톤매핑이 포함된다.** (가)는 밤에 같은 불만이 재발할
// >   여지를 남긴다 — 지시를 절반만 이행하는 형태다."*
//
// 그래서 기본값은 **조명도 톤매핑도 안 받는** 것이다(`basic` + `toneMapped:false`).
//
// **(나) `emissiveMap` 은 기각됐다** — 조명 **위에** 원본을 더하는 방식이라 밝은 곳에서
// 과노출되고, 그것은 「영향을 안 받는다」가 아니라 「영향 + 원본」이다. 감독 문언과
// 정면으로 어긋난다. 그리고 `decide/water.ts:444-447` 이 `emissive` 판정 이력을 갖고
// 있어 다시 쓰면 그 재론 비용이 붙는다.
//
// ── ⚠ 왜 노브가 남아 있는가 — 이제 「후보」가 아니라 「대조군」이다 ────────
// 팀장 조건 1 은 원래 *"기본값 (다)로 배포하되 (가)를 노브로 함께 열어 … 카드 퀴즈로
// 최종 판정을 받는다"* 였다. **감독이 두 번째 발화로 그 판정을 먼저 줬다**(문언은 위).
//
// ⚠ 카드를 생략하는 근거를 정확히 적는다(검수관 권고 P10 — 첫 판본은 *"문언이 후보 셋 중
// 하나를 지목했다"* 라고 적었고 **과했다**). 감독 문언 *"그림자 영역에서도 / 밝은 곳에서도
// 그대로"* 는 **공간적 조명 축만** 말하므로 (가)와 (다)를 가르지 못한다 — 그 문언이 배제한
// 것은 **2(예전 동작)뿐**이다. 카드를 안 올리는 진짜 이유는 **(가)/(다)를 가르는 축(시간대
// 노출)을 감독이 연 적이 없기 때문**이고, 안 연 축을 내가 열어 묻는 것이 이 저장소의
// 0번 실수다. 팀장도 이 생략에 동의했다.
//
// 그래서 1·2 는 **회귀 대조군**이다 — 감독이 고를 후보가 아니다.
//
// ── 📊 실측 — 세 값이 실제로 갈린다 (2026-08-18, 헤드리스 swiftshader) ─────
// ⚠ **이 표는 `fog:true` 시절 값이다**(검수관 권고 P9). 지금 코드는 기본값이 안개도
// 끊으므로 `artenv=0` 의 0.989 는 **1.000 에 더 가까워질 것**이다 — 방향이 보수적이라
// 결론(세 값이 갈린다)은 안 뒤집힌다. 그래서 다시 뜨지 않고 시점만 명기한다.
//
// 마젠타 단색 사진을 벽에 걸고 **낮 화면에서 그림 픽셀을 고른 뒤 그 좌표로** 밤·낮
// 밝기를 잰다(어두워서 못 고르는 문제를 구조로 없앤다 — 고르는 축과 재는 축을 분리).
//
//   artenv │ 낮    │ 밤    │ 밤/낮  │ 그림px │ 무엇을 뜻하나
//   ───────┼───────┼───────┼────────┼────────┼──────────────────────────────
//     0(다)│ 232.5 │ 230.0 │ 0.989  │  581   │ 환경 변동 **1.1%** — 요구대로
//     1(가)│ 217.3 │ 222.7 │ 1.025  │  580   │ 톤매핑만큼 2.5% 남는다
//     2    │ 129.5 │ 177.8 │ 1.373  │  393   │ **37.3%** — 감독이 본 그 차이
//
// 두 회차가 소수 셋째 자리까지 같았다(재현성 확인).
//
// ⚠⚠ **행 간(세로) 비교는 표본이 다르다 — 그대로 인용하지 마라**(검수관 권고 P4, 재지적).
// 마스크는 **낮 화면에서** 고르는데 대조군은 어두워서 통과 픽셀이 393 개로 줄었다(다른
// 값 581). 즉 «대조군 낮 129.5 는 (다) 232.5 의 56%» 같은 문장은 **서로 다른 표본의
// 평균을 나란히 놓은 것**이다. 방향은 보수적이지만(어두운 픽셀이 빠졌으므로 129.5 는
// 과대평가 — 실제 대조군은 더 어둡다) 이 저장소는 「표본이 조건에 따라 달라지는데 그
// 평균을 비교」로 이미 데였다. **행 내(가로) 비교인 밤/낮 비는 같은 좌표를 쓰므로 성립한다.**
//
// ⚠ 이 권고를 나는 한 번 **「적었다」고 보고했으나 실물에 없었다**(검수관이 다시 잡았다).
// 원거리 표에는 같은 종류의 한계를 제대로 적어 놓고, 거기서 배운 것을 옛 표에 되돌리지
// 않았다 — 보고와 실물이 어긋난 것 자체가 이 회차에서 두 번째다.
//
// ⚠ 이 표는 **밤/낮**을 잰 것이지 «그늘/양지» 를 잰 것이 아니다. 감독 문언은 그늘인데,
// 그늘을 직접 통제하는 수단이 하네스에 없어 시간대로 대신했다. 같은 벽면이 시간대에
// 따라 조명을 다르게 받으므로 **같은 축의 대리**이지만, 동일하지는 않다.
//
// ── 📊 첫 작품 걸기 계단 (팀장 조건 4, 같은 회차) ───────────────────────────
// 작품 0→1 에서 개수가 얼마나 뛰는가. 재질이 여기서 만들어지므로 표도 여기 둔다
// (`systems/artwork-scene.ts` 는 이 절을 가리키기만 한다 — 값 미러링 회피).
//
//   artenv=0  geo +2 · tex +1 · pipe **+2** · draw +2
//   artenv=1  geo +2 · tex +1 · pipe +1 · draw +2
//   artenv=2  geo +2 · tex +1 · pipe +1 · draw +2
//
// `[8]` 하늘 예열이 잡았던 것과 **같은 부류**(`info.memory` 는 첫 렌더에 오른다)이고
// 크기가 다르다 — 하늘은 세 축이 동시에 뛰었고 여기는 액자 한 개분이다. 예열을 넣지
// 않은 것은 **팀장 판정 3**(*"계단을 재기 전의 예열은 추측 구현이다"*)이고, 재고 나서도
// 이 크기면 별건을 열 근거가 안 된다. 라이브 작품은 아직 0개다.
//
// ⚠ **`artenv=0` 만 pipe +2 인 것을 재질 탓으로 단정하지 않는다**(검수관 P5). 세 세션의
// 부팅 기준선이 걸기 **전에** 이미 42/40/40 으로 달랐고(스트리밍 상태 차이), 표본이
// 회차 1회다. 귀속을 주장하려면 같은 자세·같은 파셀에서 반복해야 한다.
//
// ── ⚠⚠ 백엔드 — `toneMapped` 는 **WebGPU 에 대응 코드가 없다** (정적 실측) ──
// 이 자리에는 원래 *"백엔드 의존이 **있을 수 있다** — 감독 실기기가 유일한 축"* 이라고
// 적혀 있었다. **유보로 남길 일이 아니었다** — 브라우저 없이 `grep -c` 두 번으로 갈린다
// (검수관 블로커 B3, 2026-08-18. 내가 「원리적으로 못 잰다」로 분류한 것을 그가 쟀다):
//
//   node_modules/three/build/three.module.js  (WebGL)   `toneMapped` 참조 **4건**
//     └ `if ( material.toneMapped )` 가 셰이더 프로그램 파라미터를 가른다
//   node_modules/three/build/three.webgpu.js  (WebGPU)  `toneMapped` 참조 **0건**
//     └ 톤매핑은 `RenderOutputNode` 로 **화면 단위**에 걸린다 — 재질 단위 플래그가 없다
//
// 그래서 정확히 이렇게 갈린다:
//
//   축                   WebGL      WebGPU
//   ───────────────────  ─────────  ─────────────────────────────
//   조명·그늘 (`basic`)   안 받음     안 받음      ← **감독 문언의 본체**
//   씬 안개 (`fog`)       안 받음     안 받음
//   시간대 노출/톤매핑     안 받음     **받는다**   ← 여기만 남는다
//
// **감독 지시 셋은 두 백엔드 모두에서 이행된다** — *"재질감 전혀없이"*·*"그림자
// 영역에서도 그대로"*·*"밝은 곳에서도 그대로"* 는 전부 **공간적 조명 차이**이고,
// `MeshBasicMaterial` 이 그것을 양쪽에서 끊는다.
//
// ⚠ 남는 것은 **시간대 축**이다. `adapters/renderer.ts:162` 가 `ACESFilmicToneMapping`
// 을 켜고 `systems/night-lights.ts:80` 이 밤에 `toneMappingExposure` 를 올린다 — 즉
// **WebGPU 실기기에서는 작품 밝기가 시간대 노출을 따라간다.** 화면 전체에 똑같이 걸리는
// 축이라 «그늘/양지» 문제는 아니지만, 「완전 무관」은 아니다.
// 헤드리스 실측으로 그 폭은 **2.5%** 다(아래 표의 `artenv=1` — 톤매핑만 받는 상태).
// **이 값이 WebGPU 실기기에서도 같은지는 감독 실기기가 유일한 축이다.**
// 편차가 눈에 띈다는 보고가 오면 그때 **수단 교체를 팀장 분기로 올린다**(팀장 지시).

import { readNum } from '../url-knob.js';

/**
 * 그림 평면 재질의 명세. **테두리는 여기 없다** — 감독이 "사진은" 이라고 특정했고,
 * 테두리까지 발광시키면 액자가 공간에서 붕 뜬다(그림자·주변광과 어긋난다).
 */
export interface ArtMatSpec {
  /** `basic` 은 조명을 아예 안 받는다. `standard` 는 예전 동작(대조군) */
  readonly kind: 'basic' | 'standard';
  /**
   * `false` 면 시간대 노출·톤매핑도 안 받는다.
   *
   * ⚠ **WebGL 에서만 먹는다** — 아래 「백엔드」 절 참조.
   */
  readonly toneMapped: boolean;
  /**
   * `false` 면 **씬 안개도 안 받는다**(팀장 판정 (B), 2026-08-18).
   *
   * ── 왜 이 필드가 생겼나 ──────────────────────────────────────────────────
   * `MeshBasicMaterial` 의 `fog` 기본값은 **`true`** 다(three r171, 두 빌드 모두).
   * 즉 조명·톤매핑을 다 떼어내도 **안개는 그대로 받고 있었다** — `main.ts:435` 가
   * `scene.fog` 를 켜고 밴드는 `decide/fog.ts` 의 near 51.2m / far 76.8m 이며, 밤
   * 안개색은 `0x0b0d12`(거의 검정)이다. 먼 작품은 검게 물든다.
   *
   * 팀장 판정 근거: *"감독이 두 번 지적한 증상은 「작품이 주변환경에 물든다」이고
   * … 0번 실수(감독이 안 연 축을 내가 여는 것)가 아니라 **감독이 연 축을 덜 닫은
   * 것**이다. (A)는 같은 지적을 세 번째 받는 창을 남긴다."*
   *
   * 📊 **원거리 실측**(팀장 조건 1, 2026-08-18 밤·헤드리스). 벽에 건 작품을 정면에 둔
   * 채 **뒤로 물러나며** 그림 픽셀의 평균 밝기를 잰다. 톤매핑이 섞이지 않게 **세션 안의
   * 「근거리 대비」 비**를 쓴다 — 톤매핑은 한 세션에서 상수라 비에서 상쇄된다.
   *
   *   물러난 거리 │ fog:false(0) │ fog:true(1) │ 안개 몫
   *   ───────────┼──────────────┼─────────────┼──────────────────────────
   *      ~4.5m   │    1.017     │    0.964    │  5%
   *      ~9m     │    1.000     │    0.827    │ 17%
   *      ~12m    │    0.744     │    0.586    │ 21%
   *      ~16m    │    0.839     │    0.596    │ 29%
   *      ~22m    │    0.893     │  **묻힘**   │ 100% — 그림 픽셀이 **한 개도** 안 잡힌다
   *
   * 마지막 행이 이 판정의 근거다. `fog:true` 는 그 거리에서 작품이 **안개색에 완전히
   * 먹혀** 색 판정을 하나도 통과하지 못했고, `fog:false` 는 194px 가 남아 0.893 을
   * 유지했다.
   *
   * ⚠ **못 잰 것 둘**(통과로 적지 않는다):
   * ① 두 열 모두 거리가 늘면 값이 떨어지는데 **그 일부는 안개가 아니다** — 원근으로
   *    그림이 작아지면 가장자리 안티앨리어싱 픽셀의 비중이 커진다(표본이 670→194 로
   *    준다). 그래서 절대값이 아니라 **두 열의 차이**만 안개 몫으로 읽는다.
   * ② **카메라–작품 실거리를 못 쟀다.** 표의 거리는 「걸기 지점에서 물러난 거리」이고
   *    작품까지 실거리는 그보다 크다(조준이 먼 벽을 맞출 수 있다). 그래서 이 표를
   *    `decide/fog.ts` 의 밴드(near 51.2m)와 직접 대조하면 안 된다.
   *
   * ⚠ **재론 트리거**(팀장 조건 1): 감독이 **원거리 작품 화면을 처음 실견하는 시점**에
   * 이 판정은 뒤집힐 수 있다 — 안개 밖에서 마을은 묻히는데 **작품만 선명하게 뜨는 룩**은
   * 아직 아무도 못 봤다(라이브 작품 0개). 그때 링크로 드린다.
   */
  readonly fog: boolean;
}

/** 노브 이름. 「사진이 환경(env) 영향을 받는가」 */
export const ART_ENV_KNOB = 'artenv';
/**
 * 기본값 — 팀장 채택 (다), **감독이 문언으로 확정했다**(2026-08-18 두 번째 발화).
 *
 * ⚠ **감독 카드는 안 올린다**(팀장 동의). *"작품에는 재질감 전혀없이. 그냥 뷰어처럼
 * 밝기가 보였으면해. 그림자 영역에서도 작품은 그대로. 밝은 곳에서도 그대로."* 가
 * 후보 셋 중 하나를 지목한 것이라, 다시 묻는 것은 **이미 답한 것을 되묻는 일**이다.
 * 그래서 1·2 는 이제 「감독이 고를 후보」가 아니라 **회귀를 재는 대조군**이다.
 */
export const ART_ENV_DEFAULT = 0;
export const ART_ENV_MAX = 2;

/**
 * 노브 값 → 재질 명세. **순수** — 이 함수가 판정의 전부다.
 *
 *   0  (다) 조명·톤매핑·안개 전부 안 받는다   ← 기본값, 감독 확정
 *   1  (가) 조명만 안 받는다(톤매핑·안개는 받음)
 *   2  셋 다 받는다 — **재질 축의 대조군**
 *
 * ⚠ **2 를 「W8-7 이전과 같은 상태」로 읽지 마라**(검수관 권고 P11). 텍스처 색공간
 * 표시는 **노브 밖**이라 세 값 모두에 항상 적용된다 — 그것도 이 회차에 고친 것이다.
 * 즉 2 는 「예전 동작」이 아니라 **재질 인자만 예전과 같은 상태**다.
 *
 * ⚠ **범위 밖은 기본값으로 떨어진다**(fail-safe). 링크를 손으로 고치다 오타가 나면
 * 감독이 «왜 안 바뀌지» 를 겪는데, 그때 보이는 것이 **요구대로 동작하는 기본값**인 편이
 * 낫다 — 오타 때문에 예전 동작(어두운 사진)으로 되돌아가면 그것이 더 나쁜 오해를 낳는다.
 */
export function artMatSpec(mode: number): ArtMatSpec {
  if (mode === 2) return { kind: 'standard', toneMapped: true, fog: true };
  if (mode === 1) return { kind: 'basic', toneMapped: true, fog: true };
  return { kind: 'basic', toneMapped: false, fog: false };
}

/**
 * 지금 세션의 노브 값. `location` 이 없으면(노드) 기본값이다.
 *
 * ⚠ **읽기와 판정을 가른 이유**: 판정(`artMatSpec`)은 순수라 테스트·뮤테이션이 닿고,
 * 읽기는 URL 에 의존한다. 합쳐 두면 테스트가 URL 을 흉내 내야 하고 그 순간 축이 흐려진다.
 */
export function readArtEnv(): number {
  return readNum(ART_ENV_KNOB, ART_ENV_DEFAULT, 0, ART_ENV_MAX);
}
