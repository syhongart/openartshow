// world2/decide/shadow-decal.ts — 파츠 그림자 데칼의 **판정**. 순수 함수만.
//
// ── 왜 데칼인가 (감독 판정 2026-08-11) ──────────────────────────────────────
// 실시간 태양 그림자는 폐지했다. 이동 중 명멸했고, 감독이 `?shint=0` 화면을 *"제일 낫다"*
// 로 판정했다(경위는 `main.ts` 의 `SHADOW_INTENSITY` 주석 한 곳). 그런데 그림자가 아예
// 없으니 물건이 땅에 놓인 게 아니라 **떠 보인다** — 접지감이 사라졌다.
//
// 감독: *"그림자 베이킹해서 하는거 아냐?"* → 캐스터마다 **눕힌 평면 인스턴스**를 하나씩
// 붙이고, 실루엣은 부팅 때 구운 캔버스 아틀라스에서 읽는다. 그림자가 파츠 하나가 되므로
// 파셀 수명·tier·성장·수축·슬롯 예산이 전부 기존 구조에서 따라온다.
//
// ── 이 파일이 소유하는 것 ───────────────────────────────────────────────────
// 태양 → 지면 기저, 그림자 치수, 데칼 자세, 아틀라스 격자, 시간대 농도. **전부 산술이고
// three 를 모른다.** 굽는 쪽(`parts/shadow.ts`)과 놓는 쪽(`systems/shadow-decal.ts`)이
// 같은 함수를 보게 하는 것이 요점이다 — 한쪽이 자기 식으로 다시 계산하면 실루엣과 자세가
// 어긋나고, 그것은 화면에서 "그림자가 물건에서 떨어져 있다" 로만 드러난다.

import { DEFAULT_LAYOUT } from '../parts/types.js';
import type { SkyTime } from './night.js';

// ── 상수 — 노브의 fallback 이 되는 SSOT. `main.ts` 는 이것을 읽어 `readNum` 한다 ──

/** 그림자 농도(알파 배수). `?shdark` */
export const SHADOW_DENSITY = 0.45;

/**
 * 부드러움 손잡이 0~`SHADOW_SOFT_MAX`. `?shsoft`
 *
 * ⚠ **의미가 2026-08-11 룩 재설계에서 바뀌었다.** 예전에는 `blur(soft·res)` 의 **블러 반경**
 * 이었고, 그래서 올릴수록 실루엣 **전체**가 균일하게 흐려졌다(그리고 캔버스 경계에서 잘렸다 —
 * `SHADOW_PAD` 주석의 실측표). 지금은 **penumbra 확산 폭**이다: 실루엣을 여러 겹 등고선으로
 * 그릴 때 가장 바깥 등고선이 원본에서 얼마나 밀려나는가다. 접지부는 좁고 끝은 넓게 밀리므로
 * 같은 손잡이가 이제 "반그림자의 크기" 를 움직인다.
 *
 * **0~1 의 절대 픽셀비가 아니라 정규화 손잡이다.** 실제 확산량은 여백(`SHADOW_PAD`)이
 * 상한이고 `soft/SHADOW_SOFT_MAX` 가 그 안에서의 비율이다. 절대비로 두면 슬라이더의 대부분이
 * 여백 포화 구간에 들어가 죽는다 — 실측: 여백 0.12 · 헤드룸 0.6 이면 0.072 위로는 전부
 * 같은 그림이다(노브 범위 0~0.5 의 **86%가 죽는다**).
 */
export const SHADOW_SOFT = 0.42;

/**
 * `?shsoft` 노브 상한. `main.ts` 의 `readNum` 과 슬라이더가 **이것을 읽는다** — 0.5 를
 * 세 곳에 적어 두면 한쪽만 고쳐도 아무도 모른다(값 미러링). 확산 정규화의 분모이기도 하다.
 */
export const SHADOW_SOFT_MAX = 0.5;

/**
 * 꼬리 알파의 **바닥값**. `?shtail`
 *
 * ⚠ **의미가 2026-08-11 룩 재설계에서 바뀌었다.** 예전에는 문자 그대로 "꼬리 **끝** 알파"
 * 였고, 그래서 `shtail=0.2` 는 그림자가 알파 0.2 로 **뚝 끊기는** 것을 뜻했다(캔버스 아래
 * 경계 실측 알파 17/255, 그 바깥은 0). 지금은 꼬리 감쇠 곡선이 수렴하는 바닥이고, 마지막
 * `TAIL_FADE` 구간이 그 바닥에서 **0 까지** 한 번 더 내려간다. 즉 이 값이 무엇이든 꼬리
 * 끝에서 알파가 0 이다 — 끊기는 자리가 원리상 없어졌다.
 */
export const SHADOW_TAIL = 0.2;

/**
 * 실루엣이 셀 가장자리에서 물러나는 **여백**(셀 한 변에 대한 비, 각 변).
 *
 * ── 왜 필요한가 — 감독 반려 *"딱딱하고 실제 그림자와 너무 달라"* 의 직접 원인 ────────
 * 스크래치 캔버스의 `res × res` **전체**가 데칼 평면이 된다(`bakeAtlas` 가 그 전부를 잘라
 * 셀에 확대 합성한다). 그런데 옛 실루엣은 그 경계에 정확히 닿아 있었다 — 좌우는 `rx =
 * res·0.5`, 위는 `cy − ry = 0`, 아래는 `fillRect(…, res − cy)`. **알파가 0 으로 내려갈
 * 자리가 없으니** 블러를 아무리 걸어도 경계에서 뚝 끊긴다.
 *
 * 헤드리스 크로미움 실측(2026-08-11, `res=128`·기본값):
 *
 *   | 축                | 경계 픽셀 알파 | 셀 최대 알파 | 경계/최대 |
 *   |-------------------|---------------|-------------|----------|
 *   | 좌·우 (x=0, 127)  | 52            | 165         | 31%      |
 *   | 위   (y=0)        | 65            | 165         | 39%      |
 *   | 아래 (y=127)      | 17            | 165         | 10%      |
 *
 * `soft` 를 노브 최대(0.5)로 올린 회차는 좌 28 / 최대 39 로 **72%** 였다 — 흐리게 할수록
 * 전체가 평평해져 경계 스텝의 **비중은 오히려 커진다.** 노브로는 못 고치는 결함이다.
 *
 * ── 왜 상수 하나인가 ────────────────────────────────────────────────────────
 * 실루엣을 안쪽으로 물리면 월드에서 그림자가 그만큼 **작아진다.** 그래서 놓는 쪽이
 * `DECAL_SCALE` 로 되돌린다. 굽는 쪽과 놓는 쪽이 각각 숫자를 적으면 그것이 이 저장소가
 * 색·수치·임계값에서 세 번 데인 값 미러링이고, 증상은 "그림자가 물건보다 조금 크다/작다"
 * 로만 나타나 아무 테스트도 안 걸린다. **양쪽이 이 상수와 `decalLocalZ` 를 함께 본다.**
 *
 * ── 값의 근거 — **여백은 블러 몫만 담는다** ────────────────────────────────
 * 여백은 공짜가 아니다. 데칼 쿼드 면적이 `DECAL_SCALE²` 배가 되고, `SHADOW_MAX_LEN`
 * 주석이 못 박듯 **이 기능의 실제 비용은 드로우콜이 아니라 투명 면적**이다.
 *
 * 첫 판본은 0.12 였고 **반그림자 예산 전부를 여백에서 꺼내 썼다.** 실패했다 — 확대 렌더로
 * 보니 밑동 좌우와 `box` 골격의 위 변이 여전히 **직선으로 뚝 서 있었다.** 여백에서 낼 수
 * 있는 확산이 `res=128` 에서 4.6px(3.6%)뿐이라 눈에 안 들어온다. 여백을 키워 메우려면
 * 0.16 은 되어야 하는데 그러면 면적이 **2.16배**다.
 *
 * 그래서 반그림자를 **안쪽으로** 쓰기로 했다(`innerPx`). 코어를 실루엣 안쪽으로 줄이면
 * 예산이 여백이 아니라 **실루엣 크기**에 비례하고, 물리적으로도 그쪽이 맞다 — 그림자의
 * 바깥 경계는 penumbra 의 바깥이고 완전그늘은 그보다 안쪽이다. 여백은 그 위에 얹히는
 * 블러 꼬리와 바깥 확장(`outerPx`)만 담으면 되므로 0.07 로 충분하다 — **면적 1.35배.**
 * 첫 판본보다 반그림자는 넓어지고 비용은 줄었다.
 *
 * ⚠ **이 면적 증가는 게이트가 안 잰다** — `[7][7.6]` 은 개수와 드로우콜만 보고 필레이트를
 * 보는 축이 이 저장소에 없다. 실기기 판정이 유일한 축이다.
 */
export const SHADOW_PAD = 0.07;

/**
 * 여백만큼 작아진 실루엣을 월드에서 되돌리는 스케일 배수. `decalTransform` 이 곱한다.
 *
 * 여백이 **대칭**이라 위치 보정은 필요 없다 — 밑동 중심의 월드 오프셋을 다시 유도하면
 * `pad` 가 통째로 약분된다(`decalLocalZ` 주석에 유도가 있다). 그래서 `back` 은 그대로다.
 */
export const DECAL_SCALE = 1 / (1 - 2 * SHADOW_PAD);

/**
 * 실루엣 안의 정규화 위치 → 데칼 평면 로컬 z.
 *
 * `t = 0` 이 발밑 쪽 실루엣 끝, `t = 1` 이 그림자 끝이다. 결과는 평면 로컬 z(−0.5 ~ +0.5)
 * 이고, 여백 때문에 `t=0` 이 −0.5 가 아니라 `pad − 0.5` 에 온다.
 *
 * ── 왜 함수로 두는가 ────────────────────────────────────────────────────────
 * 굽는 쪽은 이것에 `res` 를 곱해 캔버스 y 를 얻고(`y = (decalLocalZ(t) + 0.5)·res`),
 * 놓는 쪽·테스트는 이것을 로컬 z 로 직접 쓴다. **같은 함수를 보므로 여백을 한쪽에서만
 * 고치는 일이 원리상 불가능하다.**
 *
 * ── 밑동 오프셋이 여백과 무관하다는 유도 ────────────────────────────────────
 * 밑동 중심은 실루엣 안에서 `t = blobFrac/2` 다. 그 월드 오프셋(캐스터 중심 기준)은
 *
 *     decalLocalZ(bf/2) · (len · DECAL_SCALE)
 *   = [pad + (1−2pad)·bf/2 − 0.5] · len / (1−2pad)
 *   = [−(0.5−pad)/(1−2pad) + bf/2] · len
 *   = [−0.5 + bf/2] · len                     ∵ (0.5−pad)/(1−2pad) = 0.5
 *   = −len/2 + width/2                        ∵ bf = width/len
 *
 * 즉 `back = len/2 − width/2` 는 **여백을 넣기 전과 정확히 같다.** 테스트가 이 등식을
 * 실제 three 행렬로 확인한다(`world2-shadow-decal.test.ts`).
 */
export function decalLocalZ(t: number): number {
  return SHADOW_PAD + t * (1 - 2 * SHADOW_PAD) - 0.5;
}

/**
 * 데칼이 놓이는 높이(m). 도로(0.14)보다 위여야 도로 위에도 그림자가 진다. `?shy`
 *
 * z-fighting 은 기기마다 깊이 정밀도가 달라 여기서 다 막을 수 없다 — 그래서 노브로 연다.
 * 재질 쪽 `depthWrite:false` 가 1차 방어이고 이 높이가 2차다.
 */
export const SHADOW_Y = 0.20;

/**
 * 그림자 **길이 상한**(m). 셀 한 변. `?shlen`
 *
 * ── 왜 상한이 필요한가 (팀장 조건 1 의 설계 단계 판정) ─────────────────────
 * 노을 고도는 12.3°(`sky.js` 의 `sunEl`)라 `cot ≈ 4.58` 이다. 타워(60m)의 물리적 그림자는
 * **275m — 8.6셀**이 된다. 상한 없이 두면 셋이 동시에 터진다:
 *   ① 스트리밍 가장자리에서 **로드되지 않은 땅 위**로 뻗어 허공에 뜬다.
 *   ② 데칼에는 자기 차폐가 없다 — 그 275m 띠가 도로·건물·다른 그림자를 그대로 관통해
 *      지나가고, 화면에서는 그것이 고장으로 읽힌다.
 *   ③ 투명 쿼드 면적이 8.6배가 되어 필레이트가 무너진다(이 기능의 실제 비용은 드로우콜이
 *      아니라 **투명 면적**이다. 그래서 이 상한이 곧 성능 손잡이다).
 *
 * ── 왜 클립도 페이드도 아닌 클램프인가 ─────────────────────────────────────
 * **클립**(프러스텀에서 잘라내기)은 잘린 자리가 하드컷으로 보인다. **페이드**(경계 근처
 * 알파 0)는 데칼이 파셀 경계를 알아야 하는데 데칼은 파셀을 모르고, 알게 만들면 이웃
 * 파셀의 로드 상태까지 물어야 한다 — 데칼 하나가 스트리밍을 참조하기 시작한다.
 * **클램프는 `cot` 를 자르는 것뿐**이라 실루엣이 잘리지 않고 그냥 짧아진다. 굽는 쪽과
 * 놓는 쪽이 같은 `shadowSpan` 을 보므로 어긋날 자리도 없다.
 *
 * 32m 는 데칼이 **자기 파셀의 바로 이웃 링을 넘지 않는다**는 뜻이다. 스트리밍 최외곽
 * (76.8m)에서는 그 이웃이 없을 수 있지만 그 거리의 안개율이 100% 다(`decide/fog.ts`) —
 * **재본 대가이지 짐작이 아니다.**
 *
 * ⚠ 아직 안 잰 것: 노을에서 이 상한이 **화면에 보이는가**(그림자가 부자연스럽게 뚝 끊긴
 * 것처럼 읽히는가). 감독 실기기 `?time=sunset` 판정으로 닫는다.
 */
export const SHADOW_MAX_LEN = DEFAULT_LAYOUT.cellX;

/**
 * 시간대별 농도 배수.
 *
 * 반구광 세기로 시간대를 추측하지 않는다 — `features/types.ts` 가 그 규약을 명시한다.
 * 밤에 0.35 인 것은 달빛 그림자가 실제로 옅기 때문이고, 이 값이 0 이 아닌 것은 밤에도
 * 접지감이 필요하기 때문이다(그림자가 통째로 사라지면 밤에만 물건이 다시 뜬다).
 */
export const DENSITY_BY_TIME: Readonly<Record<SkyTime, number>> = {
  day: 1, sunset: 0.8, night: 0.35,
};

// ── 룩 후보 — `?shstyle=` ───────────────────────────────────────────────────
//
// 감독 반려 2026-08-11: *"지금 그림자 너무 엉망인데.. 딱딱하고 실제 그림자와 너무 달라."*
//
// 룩은 글로 못 정한다(운영 규율 §판단이 갈리는 것). 축을 하나 잡고 후보를 동시에 열어
// 감독이 **재배포 없이 비교**하게 한다. 이 축은 **반그림자(penumbra)가 어디에 모이는가**다 —
// 「부드럽게 3단계」 같은 강약이 아니라, 같은 부드러움을 **어디에 쓰느냐**가 갈린다.
//
//   `soft`    거리에 비례해 penumbra 가 넓어진다. 접지부는 좁고 선명, 끝은 넓고 흐리다.
//             태양처럼 각지름이 작은 광원의 물리 그대로다.
//   `contact` 어둠을 접지부에 몰아준다. 밑동이 진하고 꼬리는 빠르게 옅어져 사라진다.
//             "물건이 땅에 붙어 있다" 는 신호가 가장 세고, 화면은 가장 덜 어지럽다.
//   `diffuse` penumbra 가 어디서나 넓다. 완전그늘(umbra)이 거의 없는 균일한 얼룩이다.
//             흐린 하늘·앰비언트가 지배하는 상태. 가장 눈에 안 띈다.
//
// **기본은 `soft`** — 감독이 든 불만이 *"실제 그림자와 다르다"* 이고, 실제 옥외 태양
// 그림자에서 사람이 읽는 단서가 바로 "발밑은 또렷하고 끝은 번진다" 이기 때문이다.
// `contact` 는 그 단서를 과장한 것이고 `diffuse` 는 지운 것이다.

/** 룩 후보 이름 */
export type ShadowStyle = 'soft' | 'contact' | 'diffuse';

/** 허용 목록. `readEnum` 이 이것을 받는다 — 목록을 `main.ts` 에 다시 적지 않는다 */
export const SHADOW_STYLES: readonly ShadowStyle[] = ['soft', 'contact', 'diffuse'];

/** 기본 룩. `?shstyle` 무지정일 때 나오는 것 */
export const SHADOW_STYLE: ShadowStyle = 'soft';

/**
 * 룩 하나를 결정하는 값들. **순수 데이터라 여기(판정)에 둔다** — 굽는 쪽은 이것을 읽어
 * 그리기만 하고, 자기 상수를 갖지 않는다.
 */
export interface StyleProfile {
  /**
   * penumbra 등고선 겹 수. 겹칠수록 알파 계단이 잘아진다. 이것이 곧 굽기 비용이다
   * (셀마다 이 횟수만큼 블러가 걸린다).
   */
  layers: number;
  /**
   * 접지부 penumbra 폭 ÷ 최대 penumbra 폭. 작을수록 발밑이 또렷하다.
   * 1 이면 접지부와 끝이 똑같이 번진다(= 완전그늘이 없다).
   */
  contactTight: number;
  /** 꼬리 끝 penumbra 폭 ÷ 최대 penumbra 폭. 1 을 넘으면 끝으로 갈수록 더 벌어진다 */
  flare: number;
  /**
   * 꼬리 끝에서 **완전그늘(umbra)이 좁아지는** 비(0~1). 1 이면 끝에서 코어가 소멸한다.
   *
   * ── 왜 이 축이 따로 필요한가 (첫 판본 실측으로 드러났다) ────────────────────
   * penumbra 를 바깥으로 미는 것만으로는 "끝으로 갈수록 넓어진다" 가 화면에 안 나온다.
   * 확산량이 **여백에 묶여 있어서**다 — 실측(`res=128`·기본값): 밑동 폭 106px, 꼬리 끝
   * 폭 106px 로 y30·y60·y90 이 **전부 같았다.** 확대분 11px 은 블러에 묻힌다.
   *
   * 안쪽으로 좁히는 것은 **여백을 안 쓴다.** 그래서 코어를 끝에서 좁히면 바깥 등고선과
   * 벌어져 부채꼴이 생기고, 여백 예산을 늘리지 않고도 penumbra 확대가 읽힌다. 물리적으로도
   * 이쪽이 맞다 — 거리가 멀어지면 umbra 는 줄어들다 사라지고 penumbra 만 남는다.
   */
  umbraTaper: number;
  /**
   * 코어(가장 안쪽)의 **누적** 목표 알파 0~1. 겹쳐 그린 결과가 이 값이 되도록 겹당
   * 알파를 역산한다 — 겹 수를 바꿔도 진하기가 따라 변하지 않게 하는 것이 요점이다.
   */
  core: number;
  /**
   * 꼬리 알파 감쇠 지수. 1 이면 선형, 클수록 밑동 근처에서 급격히 옅어진다.
   * 실제 그림자가 선형이 아닌 이유가 이것이다 — 차폐각이 거리의 제곱에 가깝게 줄어든다.
   */
  tailPow: number;
  /**
   * 블러 σ ÷ 등고선 간격. 1 이면 계단이 정확히 메워지고, 크면 반그림자 전체가 더 뭉개진다.
   * 간격 기준이라 `layers` 를 바꿔도 블러가 저절로 따라온다.
   */
  blurK: number;
  /**
   * 반그림자가 실루엣 **안쪽으로** 파고드는 폭 ÷ 밑동 가로 반경. 클수록 완전그늘이 작다.
   *
   * 이것이 반그림자 예산의 **주력**이다(여백에서 내는 `outerPx` 는 보조다). 안쪽으로
   * 쓰면 예산이 여백이 아니라 실루엣 크기에 비례하므로, 데칼 면적을 늘리지 않고도 넓은
   * 반그림자를 얻는다 — 근거는 `SHADOW_PAD` 주석의 실패 회차.
   */
  penumbra: number;
}

/**
 * 후보 세 개의 값.
 *
 * ⚠ 이 표의 값은 **화면으로만 판정된다.** 헤드리스에서 확인한 것은 산술 성질뿐이다
 * (경계 알파가 0 인가, 접지부가 끝보다 진한가, 폭이 끝으로 갈수록 넓어지는가).
 * "감독이 어느 것을 고르는가" 는 여기서 알 수 없다 — 그래서 셋을 동시에 연다.
 */
export const STYLE_PROFILES: Readonly<Record<ShadowStyle, StyleProfile>> = {
  // 접지 0.45 → 끝 1.00. 반그림자가 끝에서 2.2배 넓고 umbra 도 35% 좁아져 부채꼴이 뚜렷하다.
  soft: {
    layers: 12, contactTight: 0.45, flare: 1.00, umbraTaper: 0.35,
    core: 0.96, tailPow: 1.6, blurK: 1.80, penumbra: 0.34,
  },
  // 접지 0.26 으로 더 조여 발밑 코어가 가장 크다. 대신 tailPow 2.6 이라 꼬리가 금방 사라진다.
  // ⚠ `penumbra` 를 0.26 에서 올렸다 — 실측 u/p(완전그늘 폭 ÷ 전체 폭)가 접지 0.95 · 끝 0.95
  // 로 **거의 하드**였다. 접지를 강조하는 후보이지 하드 실루엣으로 돌아가는 후보가 아니다.
  contact: {
    layers: 10, contactTight: 0.26, flare: 1.30, umbraTaper: 0.40,
    core: 1.00, tailPow: 2.6, blurK: 1.60, penumbra: 0.32,
  },
  // 접지와 끝이 같은 폭이고 penumbra 0.60 이라 완전그늘이 거의 남지 않는다.
  diffuse: {
    layers: 16, contactTight: 1.00, flare: 1.05, umbraTaper: 0.85,
    core: 0.72, tailPow: 1.0, blurK: 2.00, penumbra: 0.60,
  },
};

/** 이름 → 값. 목록 밖 이름은 기본 룩으로 떨어진다(`readEnum` 이 이미 거르지만 이중 방어) */
export function styleOf(name: string): StyleProfile {
  return STYLE_PROFILES[name as ShadowStyle] ?? STYLE_PROFILES[SHADOW_STYLE];
}

/** 반그림자를 그리는 데 필요한 두 값. **한 함수에서 함께 나온다** */
export interface PenumbraPlan {
  /** 코어(완전그늘)가 원본 실루엣에서 안쪽으로 물러나는 폭(px). 반그림자의 **전부** */
  inner: number;
  /** 겹마다 거는 블러 σ(px). 등고선 계단을 메우고 실루엣 바깥을 0 으로 페이드한다 */
  blur: number;
}

/**
 * `?shsoft` 손잡이 → 반그림자 설계.
 *
 * ── 왜 한 함수인가 — **두 값이 서로 묶여 있다** ─────────────────────────────
 * 블러 σ 는 **등고선 간격**이 정해야 한다. 간격보다 작으면 겹 계단이 줄무늬로 보이고
 * (실측: 첫 판본에서 `diffuse` 9겹이 확대 렌더에 가로 줄로 남았다), 크면 반그림자가
 * 통째로 뭉개진다. 간격은 `inner / (layers − 1)` 이므로 `inner` 를 모르면 블러를 정할 수
 * 없다 — 따로 두면 `layers` 나 `penumbra` 를 만질 때마다 밴딩이 조용히 돌아온다.
 *
 * ── **여백은 블러 전용이다 — 바깥 확장을 안 쓴다** ──────────────────────────
 * 중간 판본에는 `outer`(최외곽 등고선을 실루엣 **밖으로** 미는 축)가 있었고 없앴다.
 * 여백을 블러와 나눠 쓰는 바람에 σ 상한이 등고선 간격 밑으로 눌려 **밴딩이 안 지워졌다.**
 * 게다가 `outer` 는 그림자를 실제보다 크게 만든다 — `decalTransform` 이 원본 실루엣 기준으로
 * 스케일을 잡으므로 그 몫은 보정되지 않는다.
 *
 * 없애고 나니 정의도 깔끔해졌다: **최외곽 등고선 = 원본 실루엣 = 월드에서 재는 그림자 경계.**
 * 그 바깥은 블러만으로 0 이 되고, 여백은 정확히 그 블러 꼬리(2σ)를 담는 자리다.
 *
 * @param soft `?shsoft` 값(0~`SHADOW_SOFT_MAX`)
 * @param res  스크래치 해상도(px) — 블러 상한이 여기서 나온다
 * @param rx   밑동 가로 반경(px) — 반그림자 폭이 여기서 나온다
 * @param sp   룩
 */
export function penumbraPlan(
  soft: number, res: number, rx: number, sp: StyleProfile,
): PenumbraPlan {
  const k = Math.min(1, Math.max(0, soft) / SHADOW_SOFT_MAX);
  const r = Math.max(0, rx);
  // 코어를 완전히 지우지 않는다 — 지우면 알파 역산(`1−(1−a)ⁿ`)이 의미를 잃는다.
  const inner = Math.min(r * 0.92, r * Math.max(0, sp.penumbra) * k);
  const n = Math.max(2, Math.round(sp.layers));
  /** 등고선 사이 간격. 블러 σ 가 이보다 커야 계단이 안 보인다 */
  const step = inner / (n - 1);
  // 2σ 가 여백을 넘으면 블러 자체가 잘리고, 그 자리에 하드컷이 다시 생긴다.
  const blur = Math.min(step * Math.max(0, sp.blurK), Math.max(0, SHADOW_PAD * res) * 0.5);
  return { inner, blur };
}

/**
 * 꼬리 알파 곡선. `t` 는 밑동 중심(0) → 그림자 끝(1).
 *
 * 두 성분의 곱이다:
 *   ① **감쇠** `tail + (1−tail)·(1−t)^tailPow` — 밑동이 진하고 멀어질수록 옅어진다.
 *      `tailPow > 1` 이면 밑동 근처에서 급격하다(실제 그림자의 모양).
 *   ② **끝 페이드** 마지막 `TAIL_FADE` 구간에서 0 까지. **이것이 `shtail` 의 의미를 바꾼
 *      부분이다** — 예전에는 `tail` 값 그대로 끊겼고, 이제는 무엇을 넣어도 끝이 0 이다.
 */
export function tailAlpha(t: number, tail: number, tailPow: number): number {
  /** 꼬리 끝에서 0 으로 내려가는 구간의 길이(꼬리 전체에 대한 비) */
  const TAIL_FADE = 0.22;
  const u = Math.min(1, Math.max(0, t));
  const base = Math.min(1, Math.max(0, tail));
  const decay = base + (1 - base) * Math.pow(1 - u, Math.max(0.1, tailPow));
  const fade = Math.min(1, (1 - u) / TAIL_FADE);
  return decay * fade;
}

// ── 태양 → 지면 기저 ────────────────────────────────────────────────────────

/** 그림자가 뻗는 지면 방향(단위벡터)과 길이 배수 */
export interface SunGround {
  /** 그림자가 뻗는 방향 x — 태양 수평 성분의 **반대** */
  ux: number;
  uz: number;
  /** 길이 배수 = cot(고도). 높이 h 인 물체의 그림자 길이가 `h·cot` 이다 */
  cot: number;
}

/**
 * 광원 방향에서 지면 그림자 기저를 유도한다. 지평선 아래거나 정수리면 null.
 *
 * ── 방향을 어디서 읽는가 — **미러링 0** ────────────────────────────────────
 * `SUN_AZ`·고도표를 여기로 복사하지 않는다. `systems/sky.ts` 의 `applySun()` 이 매 프레임
 * `sun.position` 과 `sun.target.position` 을 쓰고 있으므로, **그 둘의 차이를 정규화한 것**이
 * 곧 광원 방향이다. 밤에 달 방위(`MOON_AZ`)로 넘어가는 것도 공짜로 따라온다 — 태양표를
 * 복사했다면 밤에 그림자가 엉뚱한 쪽을 가리켰을 것이고, 그것은 화면에서만 드러난다.
 *
 * @param dx,dy,dz 씬 → 광원 방향(정규화 가정)
 */
export function sunGround(dx: number, dy: number, dz: number): SunGround | null {
  if (!Number.isFinite(dx) || !Number.isFinite(dy) || !Number.isFinite(dz)) return null;
  // 지평선 아래(또는 정확히 수평)면 그림자가 지면에 정의되지 않는다. 무한히 긴 그림자를
  // 상한으로 잘라 그리는 것보다 **안 그리는 편**이 정직하다 — 해가 진 순간에 세계 전체가
  // 32m 짜리 띠로 덮이는 것이 더 큰 사고다.
  if (!(dy > 1e-3)) return null;
  const h = Math.hypot(dx, dz);
  // 정수리 태양 — 수평 성분이 0 이라 방향이 정의되지 않는다. 그림자는 발밑 원이 되므로
  // 임의 방향(+Z)을 주고 `cot = 0` 으로 밑동만 남긴다.
  if (h < 1e-6) return { ux: 0, uz: 1, cot: 0 };
  return { ux: -dx / h, uz: -dz / h, cot: h / dy };
}

// ── 그림자 치수 ─────────────────────────────────────────────────────────────

/** 그림자 하나의 치수. **굽는 쪽과 놓는 쪽이 이것을 공유한다** */
export interface ShadowSpan {
  /** 전체 길이(m) = 밑동 지름 + 뻗는 길이 */
  len: number;
  /** 폭(m) = 밑동 지름 */
  width: number;
  /**
   * 전체 길이에서 밑동 지름이 차지하는 비(0~1).
   *
   * **이 값이 이 설계에서 가장 조용히 어긋나기 쉬운 자리다.** 캔버스는 이것으로 밑동
   * 블롭의 크기를 정하고, 자세는 이것으로 평면을 캐스터에 정렬한다. 페인터가 자기 상수로
   * 계산하면 그림자 밑동이 물건에서 떨어져 나오는데, 산술 테스트는 양쪽 다 통과한다.
   * 그래서 **한 함수에서 함께 나온다.**
   */
  blobFrac: number;
}

/**
 * 캐스터 하나의 그림자 치수를 유도한다.
 *
 * @param h      캐스터 높이(m)
 * @param r      캐스터 밑동 반경(m)
 * @param g      `sunGround` 결과
 * @param maxLen 뻗는 길이의 상한(m). `SHADOW_MAX_LEN` 또는 `?shlen`
 */
export function shadowSpan(h: number, r: number, g: SunGround, maxLen: number): ShadowSpan {
  const rr = Math.max(0.05, r);
  const cast = Math.min(Math.max(0, h) * g.cot, Math.max(0, maxLen));
  const len = rr * 2 + cast;
  return { len, width: rr * 2, blobFrac: (rr * 2) / len };
}

// ── 데칼 자세 ───────────────────────────────────────────────────────────────

/** 데칼 인스턴스 하나의 자세. y 는 호출자가 `SHADOW_Y` 로 준다 */
export interface DecalPose {
  x: number;
  z: number;
  ry: number;
  sx: number;
  sz: number;
}

/**
 * 데칼 평면의 자세를 유도한다.
 *
 * ── 좌표 규약 — **여기가 부호를 손으로 유도하면 반반 확률로 틀리는 자리다** ──
 * 데칼 지오는 `PlaneGeometry(1,1).rotateX(-π/2)` 다. 회전을 실제로 풀어 보면
 * `(x, y, 0) → (x, 0, -y)` 이므로 **평면의 +Y(=UV v=1)가 로컬 −Z 로 간다.**
 * 여기에 `CanvasTexture` 의 `flipY`(기본 true)가 겹쳐 **캔버스 픽셀 y=0(셀 위쪽)이
 * v=1, 즉 로컬 −Z** 에 대응한다.
 *
 * 그래서 규약을 이렇게 못 박는다:
 *
 *     캔버스 셀 **위쪽** = 발밑 = 로컬 −Z        캔버스 셀 **아래쪽** = 그림자 끝 = 로컬 +Z
 *
 * 즉 그림자는 **로컬 +Z 방향으로 뻗는다.** three 의 Y축 회전 θ 는 로컬 (0,0,1) 을 월드
 * `(sinθ, 0, cosθ)` 로 보내므로, 그것을 `(ux, uz)` 와 같게 두면 **`ry = atan2(ux, uz)`** 다.
 *
 * 위치는 **밑동 중심이 캐스터 중심에 오도록** 민다. 밑동 중심은 평면 로컬 z 로
 * `−0.5 + blobFrac/2` 지점이고, 월드로는 중심에서 `(−len/2 + r)` 만큼이다. 뒤집으면
 * `평면중심 = 캐스터중심 + (len/2 − r)·(ux, uz)`.
 *
 * ⚠ 이 유도가 맞는지는 **산술로 확인되지 않는다** — 부호를 뒤집어도 식은 그럴듯하고,
 * 화면 증상은 "그림자가 해 쪽에 있다" 라 눈에는 즉시 보인다. 그래서 테스트가 실제
 * three 행렬을 조립해 실루엣 끝(`decalLocalZ(1)`)의 월드 위치를 본다
 * (`world2-shadow-decal.test.ts`).
 *
 * ── 스케일이 치수보다 `DECAL_SCALE` 배 크다 ─────────────────────────────────
 * ⚠ **2026-08-11 룩 재설계로 바뀐 지점이다.** 예전에는 `sx = width`·`sz = len` 이었다.
 * 지금은 실루엣이 캔버스 안쪽으로 `SHADOW_PAD` 만큼 물러나 있으므로, 평면을 그 비율만큼
 * 키워야 **실루엣의 월드 크기가 예전과 같아진다.** 즉 이 배수는 그림자를 키우는 것이
 * 아니라 여백을 상쇄하는 것이고, 화면의 그림자 크기는 변하지 않는다 — 테스트가
 * `decalLocalZ(1)` 의 월드 거리가 `len − width/2` 로 **옛 값 그대로**임을 단언한다.
 *
 * 위치(`back`)는 여백이 대칭이라 보정이 필요 없다(`decalLocalZ` 주석의 유도).
 */
export function decalTransform(
  casterX: number, casterZ: number, span: ShadowSpan, g: SunGround,
): DecalPose {
  const back = span.len / 2 - span.width / 2;
  return {
    x: casterX + g.ux * back,
    z: casterZ + g.uz * back,
    ry: Math.atan2(g.ux, g.uz),
    sx: span.width * DECAL_SCALE,
    sz: span.len * DECAL_SCALE,
  };
}

// ── 아틀라스 격자 ───────────────────────────────────────────────────────────

export interface AtlasCell {
  /** 캔버스 픽셀 사각 */
  px: number; py: number; size: number;
  /** UV 사각(0~1). 지오메트리에 구워 넣는다 */
  u0: number; v0: number; u1: number; v1: number;
}

export interface AtlasGrid {
  cols: number;
  cellPx: number;
  cellOf(i: number): AtlasCell;
}

/**
 * 아틀라스 격자를 유도한다. 셀 수가 늘어도 넘치지 않는다.
 *
 * 격자를 `4×4` 처럼 **고정으로 적지 않는 이유**: 캐스터 파츠는 늘어난다(다리·간판 같은
 * 것이 백로그에 있다). 고정 격자는 아홉 번째 캐스터가 생기는 날 조용히 남의 셀을 읽고,
 * 그 증상은 "특정 물건의 그림자만 이상하다" 로 나타난다.
 *
 * ⚠ 셀 픽셀에 하한을 두지 않는다 — 캐스터가 아주 많아지면 셀이 작아져 실루엣이 뭉개진다.
 * 그때 늘려야 하는 것은 아틀라스 크기이고, 그 판단을 여기서 삼키면 안 된다. 대신
 * 레지스트리 테스트가 `cellPx` 하한을 단언해 **넘어가는 순간 빨간불**이 되게 한다.
 */
export function atlasGrid(count: number, atlasPx: number): AtlasGrid {
  const n = Math.max(1, Math.ceil(count));
  const cols = Math.ceil(Math.sqrt(n));
  const cellPx = Math.floor(atlasPx / cols);
  return {
    cols,
    cellPx,
    cellOf(i: number): AtlasCell {
      const cx = i % cols;
      const cy = Math.floor(i / cols);
      const px = cx * cellPx;
      const py = cy * cellPx;
      // UV 를 셀 경계에서 **반 텍셀 안쪽으로** 물린다. 경계를 정확히 쓰면 선형 필터링이
      // 이웃 셀을 반 텍셀 빨아들여, 밑동 옆에 남의 그림자 자락이 비친다(밉맵에서 더 심하다).
      const inset = 0.5 / atlasPx;
      return {
        px, py, size: cellPx,
        u0: px / atlasPx + inset,
        v0: 1 - (py + cellPx) / atlasPx + inset,
        u1: (px + cellPx) / atlasPx - inset,
        v1: 1 - py / atlasPx - inset,
      };
    },
  };
}

// ── 농도 ────────────────────────────────────────────────────────────────────

/** 시간대 배수를 곱한 최종 농도. 0~1 로 잘린다 */
export function densityFor(time: SkyTime, base: number): number {
  const m = DENSITY_BY_TIME[time] ?? 1;
  return Math.min(1, Math.max(0, base * m));
}
