// world2/decide/grid.ts — 세계는 어디까지이고 어느 칸이 무엇인가. 순수 함수만, import 0.
//
// ── 감독 지시 ────────────────────────────────────────────────────────────────
// *"월드 1처럼 격자로 해주고. 길. 땅영역. 바다. 강. 해주고. 가로 30. 세로 30 으로 하자.
//   칸의 크기는 모두 같은 크기가 아니라 조금 달랐으면해. 2개 셀이 한개인것도 있고.
//   그리고 중앙은 중앙광장으로 하고 분수대 시계탑."*
//
// ── 왜 이 파일이 SSOT 인가 ───────────────────────────────────────────────────
// "여기가 세계 안인가"를 묻는 곳이 여럿이다 — 스트리밍(파셀을 담을까)·미니맵(그릴까)·
// 물(바다인가)·배치(지을까). 각자 적으면 격자를 30에서 40으로 바꿀 때 한쪽만 따라오고,
// 증상은 **지도에는 있는데 걸어가면 없는 땅**으로 나타난다.
//
// 도로 위상(`parts/road-topology.ts`)이 경계 좌표 해시로 이웃 조회를 없앤 것과 같은
// 배치다 — 여기도 `(px,pz)` 하나로 답이 나오고 이웃을 묻지 않는다.

/**
 * 격자 크기(칸). **world5 는 20×20 이다** — 감독이 고른 "10×10 블록" 을 이 코드의
 * 블록 단위로 옮긴 값이다.
 *
 * ── 왜 10 이 아니라 20 인가 (단위를 맞춘 것이지 지시를 늘린 것이 아니다) ───
 * 감독 카드 선택: *"권장으로 10*10부터 해보자."* 그 카드가 붙어 있던 참고안은
 * **맨해튼 블록**(약 80m × 250m)을 단위로 *"꽉 찬 6~9블록"* 을 권하고 있었다.
 *
 * 그런데 이 코드의 파셀은 32m 정사각이고, **블록은 파셀이 아니라 2×2 슈퍼셀**(64m)
 * 이다 — 이 파일 아래 `blockPattern` 이 그 단위를 정의한다. 그러니
 *
 *     10×10 블록 = 20×20 파셀 = **640m 사방**
 *
 * 이고, 이것이 참고안의 6~9블록(480~720m)과도 정확히 겹친다. 파셀 10×10 으로 읽으면
 * 320m — 참고안이 권한 것보다 **오히려 작아지고**, 감독이 인용한 근거와 어긋난다.
 *
 * ⚠️ **이 해석이 틀렸으면 여기 한 줄이다.** `20` 을 `10` 으로 바꾸면 세계가 320m 가
 * 되고 나머지(바다 경계·강 폭·도심 반경·공원)는 전부 이 값에서 유도되므로 저절로
 * 따라온다. 감독 화면 판정에서 *"너무 크다"* 가 나오면 그렇게 한다.
 *
 * ── 실측 ────────────────────────────────────────────────────────────────
 *   30×30 (world2)  960m  걸어서 약 3분   ← 포크 원본
 *   **20×20 (여기)  640m  걸어서 약 2분**
 *   10×10           320m  걸어서 약 1분   ← 파셀로 읽었을 때
 *
 * (`WALK_SPEED` 5m/s 기준. 실제로는 도로를 따라 도므로 더 걸린다.)
 *
 * 줄인 이유는 참고안이 적은 그대로다 — *"텅 빈 오픈월드는 금방 지루해지고, 모바일에서
 * 특히 그렇다."* 960m 를 랜드마크로 채우려면 랜드마크가 흔해져야 하고, 흔한 랜드마크는
 * 랜드마크가 아니다.
 */
export const GRID_W = 20;
export const GRID_H = 20;

/**
 * 파셀 좌표는 **중앙이 원점**이다. 20칸이면 -10 … 9(30칸이면 -15 … 14).
 *
 * 0-기반(0…29)으로 두지 않는 이유는 스폰 때문이다. 플레이어는 `(0,0)` 에서 시작하고
 * 중앙 광장도 원점 둘레에 있어야 하는데, 0-기반이면 스폰이 세계의 **모서리**가 된다.
 */
export const GRID_MIN_X = -Math.floor(GRID_W / 2);
export const GRID_MAX_X = GRID_MIN_X + GRID_W - 1;
export const GRID_MIN_Z = -Math.floor(GRID_H / 2);
export const GRID_MAX_Z = GRID_MIN_Z + GRID_H - 1;

/** 이 파셀이 세계 안인가. **밖은 전부 바다다**(유한 세계 — 감독 확정) */
export function inGrid(px: number, pz: number): boolean {
  return px >= GRID_MIN_X && px <= GRID_MAX_X && pz >= GRID_MIN_Z && pz <= GRID_MAX_Z;
}

/**
 * 중앙 광장의 반지름(칸). 1이면 3×3 이다.
 *
 * ── 2×2 로 잡았다가 3×3 으로 고쳤다 ─────────────────────────────────────────
 * 처음엔 `(-1,0)×(-1,0)` 2×2 로 두고 "그래야 광장 중심이 월드 원점에 온다"고 적었다.
 * **틀렸다.** 파셀 `(px,pz)` 의 중심은 `(px·cell, pz·cell)` 이므로 그 2×2 의 한가운데는
 * `(-cell/2, -cell/2)` = **(-16,-16)** 이다. 원점이 아니라 광장 모서리 쪽으로 치우친
 * 자리이고, 그대로 두면 분수대가 광장 한복판에 안 선다.
 *
 * 홀수 변으로 잡으면 가운데 칸이 존재하고 그 칸의 중심이 곧 원점이다. 분수대를 그
 * 칸에 놓기만 하면 정확히 광장 한가운데에 온다 — 오프셋 계산이 필요 없다.
 *
 * 3×3 은 96m. **640m 세계의 15%**라 world2(960m 의 10%)보다 상대적으로 더 크다 —
 * 격자를 줄이면서 이 값은 안 따라온 것이고, 그 대가가 실측으로 드러났다: 스폰에서
 * 파셀이 로드되는 반경 안의 **정면 시야에 도시 파셀이 0개**다.
 *
 * ⚠️ **그 반경은 67.2m 다 — 76.8m 가 아니다**(2026-08-09 실측 정정). 이 줄은 오래
 * `DEFAULT_BANDS.farExit`(2.40셀 = 76.8m)를 적고 있었는데 그것은 **이미 far 인 파셀이
 * 유지되는** 히스테리시스 한계이고, 처음 로드되는지를 정하는 것은 `farEnter`
 * (2.10셀 = 67.2m)다(`tierFor(dist, null)` 은 ENTER 만 본다). 넓은 쪽을 적어 둔 탓에
 * 정면 목록이 실제보다 한 칸 길었다 — 공원 `(0,-2)`(74m)는 **첫 화면에 없다.**
 *
 * 실측(정정 후): 정면(-z, ±40°)에 로드되는 파셀은 `(0,0)`·`(0,-1)`·`(±1,-1)`
 * **네 칸이고 전부 중앙 광장**이다. 로드 반경 안 14칸을 다 훑어도 광장·공원·물을 뺀
 * 도시 칸은 `(±2,0)` 둘뿐이다.
 *
 * ⚠️ 그런데도 줄이지 않는다 — 1칸(32m)이면 분수대와 광고 타워가 서로를 가리고, 중앙
 * 광장은 유일한 스폰 지점이다. **처방은 광장을 줄이는 것이 아니라 채우는 것**이다
 * (감독 지시 *"타임스퀘어도 하고"*). 그 처방이 2026-08-09 에 붙었다 — 아래
 * `plazaRimSides` 가 광장의 바깥 변 12곳을 자리로 내주고 `parts/adfacade.ts` 가
 * 거기에 광고 파사드를 세운다. 광장이 광고 스크린으로 둘러싸이면 같은 크기가 빈 곳이
 * 아니라 **방**이 된다.
 */
export const PLAZA_R = 1;

/**
 * 이 파셀이 **중앙 광장**인가. 세계에 하나뿐이다.
 *
 * `parts/plaza.ts` 에도 `isPlaza` 가 있는데 그쪽은 **동네 광장**(확률 1.9%, 걷다 만나는
 * 빈 구획)이라 다른 것이다. 이름이 겹쳐 헷갈리므로 여기는 `Central` 을 붙였다.
 */
export function isCentralPlaza(px: number, pz: number): boolean {
  return Math.abs(px) <= PLAZA_R && Math.abs(pz) <= PLAZA_R;
}

/**
 * 중앙 광장 안에서 이 칸이 어느 자리인가. 광장이 아니면 `null`.
 *
 * **무엇이 서는지는 여기서 정하지 않는다.** 파츠 이름을 이 파일에 적었다가
 * "레지스트리 밖에 파츠 목록을 다시 적지 않는다" 검사에 걸렸다 — 옳은 지적이다.
 * 격자는 자리를 알고, 그 자리에 무엇을 세울지는 `parts/plaza.ts` 가 정한다.
 */
export type PlazaCell = 'center' | 'north' | 'west' | 'edge';

export function centralPlazaCell(px: number, pz: number): PlazaCell | null {
  if (!isCentralPlaza(px, pz)) return null;
  if (px === 0 && pz === 0) return 'center';
  if (px === 0 && pz === -PLAZA_R) return 'north';
  if (px === PLAZA_WEST.px && pz === PLAZA_WEST.pz) return 'west';
  return 'edge';
}

/**
 * 광장 서쪽 칸의 파셀 좌표. **자리만 정하고 무엇이 서는지는 모른다**(위 규율 그대로).
 *
 * 좌표를 상수로 빼 둔 이유는 소비자가 둘이기 때문이다 — 위 `centralPlazaCell` 과, 이
 * 칸에 무언가를 세우는 쪽. 양쪽이 `(-1, 0)` 을 각자 적으면 `PLAZA_R` 이 바뀌는 날 한쪽만
 * 따라온다.
 */
export const PLAZA_WEST = { px: -PLAZA_R, pz: 0 } as const;

// ── 광장 테두리 — 타임스퀘어의 자리 (감독 지시 2026-08-08 *"타임스퀘어도 하고"*) ──
//
// 위 `centralPlazaCell` 은 **칸이 어떤 역할인가**를 답한다(분수대 칸·시계탑 칸·…).
// 여기 것은 다른 질문이다: **이 칸의 어느 변이 광장의 바깥 경계인가.**
//
// 두 질문을 한 함수로 못 합치는 이유는 답의 개수가 다르기 때문이다. 역할은 칸마다
// 하나지만 경계 변은 모서리 칸에서 **둘**이다((1,-1) 은 북쪽과 동쪽이 다 바깥이다).
// `PlazaCell` 에 `'corner-ne'` 같은 값을 늘려 풀 수도 있었지만, 그러면 격자가 "모서리
// 에는 두 장이 선다" 는 **파츠의 사정**을 알게 된다 — 이 파일 머리말이 금지한 방향이다.
//
// **무엇이 서는지는 여전히 여기서 정하지 않는다.** 격자는 "이 변이 바깥이다" 까지만
// 답하고, 그 변에 광고 파사드를 세울지 가로수를 심을지는 `parts/` 가 정한다.

/** 광장 칸의 바깥 경계 방향. `z` 음수가 북이다(`PARK_Z` 주석과 같은 규약) */
export type PlazaRimSide = 'north' | 'south' | 'east' | 'west';

/**
 * 이 광장 칸에서 **광장 바깥과 맞닿은 변들**. 광장이 아니거나 안쪽 칸이면 빈 배열.
 *
 * 3×3 이면 가운데 칸만 빈 배열이고, 변 칸이 하나씩·모서리 칸이 둘씩 내어 **총 12 변**이
 * 나온다. 그 12 가 광장 둘레를 한 바퀴 두르는 수다 — 한 변당 한 칸씩 네 변에 3칸.
 *
 * ⚠️ **부호가 아니라 절댓값 경계로 판정한다.** `px === PLAZA_R` 만 쓰면 동쪽만 잡히고
 * 서쪽이 빠지는데, 증상은 "광장 한쪽만 뚫려 있다" 로 나타나 광장 안에 서 있으면
 * 알아채기 어렵다(등 뒤가 비어 있어도 화면에는 안 나온다).
 */
export function plazaRimSides(px: number, pz: number): readonly PlazaRimSide[] {
  if (!isCentralPlaza(px, pz)) return [];
  const out: PlazaRimSide[] = [];
  if (pz === -PLAZA_R) out.push('north');
  if (pz === PLAZA_R) out.push('south');
  if (px === -PLAZA_R) out.push('west');
  if (px === PLAZA_R) out.push('east');
  return out;
}

// ── 랜드마크 마천루의 자리 (감독 지시 2026-08-08 *"뉴욕 상징물을 넣어줘"*) ──────
//
// ── 왜 확률이 아니라 좌표인가 ───────────────────────────────────────────────
// `parts/zoning.ts` 의 `isTowerParcel` 은 좌표 해시 확률이다. 그것으로는 **"세계에 딱
// 한 기"** 를 만들 수 없다 — 확률을 아무리 낮춰도 0기이거나 2기일 수 있고, 그 수는
// 도심 프리셋을 바꿀 때마다 달라진다. 랜드마크가 둘이면 랜드마크가 아니고, 0이면
// 감독 지시가 화면에 없다.
//
// 그래서 `centralPlazaCell` 과 같은 방식을 쓴다: **격자가 좌표로 못 박는다.**
//
// ── 왜 (PLAZA_R+1, 0) 인가 — 실측에서 나온 유일한 자리다 ────────────────────
// 스폰(0,10)에서 파셀이 로드되는 반경은 `DEFAULT_BANDS.farEnter`(2.10셀 = **67.2m**)다.
// 그 안의 파셀을 전수로 훑으면 14칸이고, **광장·공원·물을 빼고 남는 것은 (±2,0) 둘뿐**
// 이다(나머지는 전부 중앙 광장 3×3 아니면 물가다). 즉 이 세계에서 "스폰에서 처음부터
// 로드되는 도시 칸" 은 좌우 두 칸이 전부이고, 랜드마크는 그중 하나여야 한다.
//
// 동쪽을 고른 이유는 서쪽이 이미 점유돼 있기 때문이다 — `parts/plaza.ts` 의
// `plazaOccupied` 가 광장 서쪽 칸을 미술관 GLB 자리로 잡고 있고(`features/glb-city.ts`
// 가 기본 노출이다), 큰 것 둘을 같은 쪽에 몰면 반대편이 비어 도시가 한쪽으로 기운다.
//
// ⚠️ **그래도 스폰 첫 화면(정지·`yaw=0`)에는 안 들어온다.** 이 칸의 방위각이 81°인데
// 화면 수평 반각은 데스크톱 16:9 에서 51°, 모바일 세로에서 18°다(수직 FOV 70° 기준).
// **고개를 돌려야 보인다** — 이것은 값을 잘못 골라서가 아니라 중앙 광장 3×3(96m)이
// 로드 반경(67.2m)을 통째로 먹기 때문이고, 광장을 줄이지 않는 한 구조적으로 그렇다
// (`PLAZA_R` 주석이 그 거래를 기록하고 있다). 정면을 채우는 일은 광장 자체를 채우는
// 쪽(위 `plazaRimSides`)이 맡는다.
//
// `PLAZA_R` 에서 유도하므로 광장이 커지면 랜드마크도 한 칸 밖으로 따라 나간다.
// 좌표를 직접 적으면 광장을 넓히는 날 랜드마크만 광장 **안**에 남는다.
export const LANDMARK_PARCEL = { px: PLAZA_R + 1, pz: 0 } as const;

/** 이 파셀에 랜드마크 마천루가 서는가. **세계에 하나뿐이다** */
export function isLandmarkParcel(px: number, pz: number): boolean {
  return px === LANDMARK_PARCEL.px && pz === LANDMARK_PARCEL.pz;
}

/** 중앙 광장 한가운데의 월드 좌표. 홀수 변이라 가운데 칸의 중심이고, 그것이 원점이다 */
export function plazaCenter(): { x: number; z: number } {
  return { x: 0, z: 0 };
}

/**
 * 플레이어가 세계에 처음 서는 자리.
 *
 * ── 왜 원점이 아닌가 ────────────────────────────────────────────────────────
 * 원점은 **분수대가 서는 자리**다. 광장 중심을 원점에 맞추면서(그래야 분수대가 오프셋
 * 없이 한복판에 선다) 스폰도 같은 좌표에 남아 있었고, 이 프로젝트에는 플레이어-메시
 * 충돌이 없으므로 아무것도 밀어내지 않는다. **부팅 첫 프레임부터 카메라가 분수대 안에
 * 있었다.** 눈높이 1.7m 가 분수대 꼭대기 1.70m 와 소수점까지 겹치기까지 했다 — 하나는
 * 라이브 미술관과 통일한 눈높이, 하나는 첨탑 조형이라 서로 모르는 값인데 그렇게 됐다.
 *
 * 검수관이 잡았다. **좌표 판정은 전부 초록이었다** — 광장이 뭍인가, 분수대가 어느 칸에
 * 서는가는 다 맞았다. 계산된 좌표가 실제 지오메트리·카메라와 만나는 지점을 아무 테스트도
 * 안 봤다. 판정/집행 경계가 또 뚫린 자리다.
 *
 * ── z = +10 인 이유 ─────────────────────────────────────────────────────────
 * 시선 기본값 `yaw = 0` 은 **-z 방향**을 본다(`facing(0) = (0,-1)`). 그러니 +z 쪽에
 * 서면 원점을 정면으로 보게 되고, 그 자리에 분수대가 있다. 그 너머 북쪽 칸에 시계탑이
 * 서므로 **분수대를 앞에 두고 시계탑을 배경으로 보는 구도**가 첫 화면이 된다.
 *
 * 10m 는 분수대 밑동(2.4m)의 네 배 남짓이라 안전하면서, 분수대가 화면에서 작아지지
 * 않는 거리다. 안전 여유는 `tests/world2-grid.test.ts` 가 `FOUNTAIN_R` 과 비교해 지킨다.
 */
export const SPAWN = { x: 0, z: 10 } as const;

// ── 블록 — "칸 크기가 조금 달랐으면" ────────────────────────────────────────
//
// 감독이 원한 것은 **모든 구획이 똑같은 정사각형이 아닌 도시**다. 실제 도시는 한 블록이
// 옆 블록의 두 배이기도 하고, 그 불규칙이 격자를 격자처럼 안 보이게 만든다.
//
// 병합을 "이 파셀이 오른쪽 이웃과 합쳐지는가"로 풀면 **충돌**이 생긴다 — A가 B와 합치고
// 싶은데 B는 이미 C와 합쳐진 상태일 수 있고, 그걸 알려면 이웃을 조회해야 한다. 파셀이
// 서로를 모른다는 이 아키텍처의 전제가 깨진다.
//
// 그래서 **2×2 슈퍼셀**을 단위로 삼는다. 슈퍼셀 하나가 자기 내부를 어떻게 나눌지 혼자
// 정하므로 이웃 조회가 필요 없고 충돌이 원리적으로 불가능하다.
//
//   슈퍼셀 안의 네 칸        내부 경계 4개
//     A B                     A|B (위 수직) · C|D (아래 수직)
//     C D                     A/C (왼 수평) · B/D (오른 수평)
//
// 경계를 끄면 두 칸이 한 블록이 된다. 패턴은 다섯 가지다:
//
//   NONE  40%  네 칸 그대로        — 기본. 대부분은 한 칸이어야 "조금 다른" 것이 된다
//   H_TOP 15%  A|B 끔 → 가로 2칸 + 낱칸 2
//   H_BOT 15%  C|D 끔
//   V_LEFT 15% A/C 끔 → 세로 2칸 + 낱칸 2
//   V_RIGHT 15% B/D 끔
//
// 2×2 통짜(네 경계 다 끔)는 넣지 않았다. 그 크기의 열린 구획은 **광장**이 맡고, 도시
// 곳곳에 광장만 한 빈 블록이 생기면 중앙 광장이 특별해 보이지 않는다.

/** 슈퍼셀 분할 패턴 */
export const enum BlockPattern {
  None = 0,
  HorizTop = 1,
  HorizBottom = 2,
  VertLeft = 3,
  VertRight = 4,
}

/** 32비트 해시. `road-topology.ts`·`parcel-layout.ts` 와 같은 알고리즘이되 소금이 다르다 */
function h2(a: number, b: number, salt: number): number {
  let h = salt >>> 0;
  h = Math.imul(h ^ (a | 0), 0x85ebca6b);
  h ^= h >>> 13;
  h = Math.imul(h ^ (b | 0), 0xc2b2ae35);
  h ^= h >>> 16;
  h = Math.imul(h, 0x27d4eb2d);
  return (h ^ (h >>> 15)) >>> 0;
}

const SALT_BLOCK = 0x6a09e667;

// ── 음수 좌표 ────────────────────────────────────────────────────────────────
// 원점이 세계 한복판이므로 **좌표의 절반이 음수다.** 정수 나눗셈을 잘못 쓰면 세계의
// 절반만 깨지고, 그건 원점 근처만 보는 검사로는 안 잡힌다.
//
// 위험한 쪽은 **`floorDiv` 다.** `floorDiv(-1,2)` 는 -1 인데 `Math.trunc(-1/2)` 는 0 이라,
// 홀수 `pz` 에서 위 슈퍼셀이 아래 슈퍼셀의 패턴을 본다. 증상은 **블록이 반쪽만 병합되는
// 것** — 한 슈퍼셀의 위 행은 합쳐졌는데 아래 행은 안 합쳐진 모습이다.
//
// `floorMod` 는 **여기서는 `%` 로 바꿔도 결과가 같다.** 쓰이는 곳이 `=== 0`(짝수인가)
// 판정뿐이고, 짝수 판정은 `a % 2 === 0` 으로도 부호와 무관하게 정확하기 때문이다.
// 그래도 남겨두는 이유는 슈퍼셀 크기를 2가 아닌 값으로 바꿀 때다 — 그때는
// `floorMod(a,3) === 1` 같은 판정이 생기고, 거기서는 `%` 가 음수에서 어긋난다.
//
// **이 주석은 뮤테이션이 고쳐 쓰게 했다.** 원래 여기에는 "`%` 를 그대로 쓰면 도시가 두
// 겹으로 갈라진다"고 적혀 있었는데, `floorMod` 를 `%` 로 바꾸는 뮤테이션이 테스트를
// **전부 통과했다.** 근거가 틀렸던 것이고, 그 자리에 진짜 위험한 `floorDiv` 를 겨냥한
// 검사(`tests/world2-grid.test.ts` 의 "같은 슈퍼셀의 두 행/열이 하나의 패턴을 공유한다")를
// 새로 넣었다.
function floorDiv(a: number, b: number): number { return Math.floor(a / b); }
function floorMod(a: number, b: number): number { return a - floorDiv(a, b) * b; }

/** 이 슈퍼셀이 자기 내부를 어떻게 나누는가 */
export function blockPattern(sx: number, sz: number): BlockPattern {
  const r = h2(sx, sz, SALT_BLOCK) / 4294967296;
  if (r < 0.4) return BlockPattern.None;
  if (r < 0.55) return BlockPattern.HorizTop;
  if (r < 0.7) return BlockPattern.HorizBottom;
  if (r < 0.85) return BlockPattern.VertLeft;
  return BlockPattern.VertRight;
}

/**
 * 파셀 `(px,pz)` 와 `(px+1,pz)` 사이 **수직** 경계에 길이 있는가.
 *
 * 슈퍼셀 **사이**의 경계는 언제나 길이다 — 그래야 도시가 격자로 이어진다. 슈퍼셀
 * **안**의 경계만 패턴이 끌 수 있고, 그것이 곧 가로 2칸 블록이다.
 */
export function gridEdgeX(px: number, pz: number): boolean {
  if (floorMod(px, 2) !== 0) return true;          // 슈퍼셀 사이 — 항상 길
  const pattern = blockPattern(floorDiv(px, 2), floorDiv(pz, 2));
  const top = floorMod(pz, 2) === 0;               // 슈퍼셀 안에서 위 행인가
  if (pattern === BlockPattern.HorizTop) return !top;
  if (pattern === BlockPattern.HorizBottom) return top;
  return true;
}

/** 파셀 `(px,pz)` 와 `(px,pz+1)` 사이 **수평** 경계에 길이 있는가 */
export function gridEdgeZ(px: number, pz: number): boolean {
  if (floorMod(pz, 2) !== 0) return true;          // 슈퍼셀 사이 — 항상 길
  const pattern = blockPattern(floorDiv(px, 2), floorDiv(pz, 2));
  const left = floorMod(px, 2) === 0;              // 슈퍼셀 안에서 왼 열인가
  if (pattern === BlockPattern.VertLeft) return !left;
  if (pattern === BlockPattern.VertRight) return left;
  return true;
}

// ── 센트럴파크 (감독 지시 2026-08-08 *"센트럴파크 공원도 넣고"*) ────────────
//
// 감독은 두 가지를 따로 말했다: 카드로 *"중앙에 호수도 넣어줘. 호수 3개 영역"*,
// 그 뒤에 *"센트럴파크 공원도 넣고"*. **둘을 하나로 합쳤다** — 실제 센트럴파크에도
// 호수가 셋 있다(남쪽 연못·가운데 호수·북쪽 저수지). 따로 두면 도시 한가운데에 공원과
// 호수가 각각 자리를 차지해 20×20 세계에서 뭍이 남지 않는다.
//
// **세로로 긴 직사각형**인 것이 이 공원의 형태다. 실제 센트럴파크가 그렇고, 격자 도시
// 한가운데 가로세로 같은 정사각 공원을 두면 그것은 공원이 아니라 빈 광장으로 읽힌다.

/**
 * 공원의 파셀 범위 — `[최소, 최대]` 닫힌 구간.
 *
 * 중앙 광장(원점 둘레 3×3)의 **북쪽**에 붙인다. 광장과 겹치면 스폰 첫 화면이 잔디가
 * 되고, 감독이 정한 구도(*분수대를 앞에 두고 시계탑을 배경으로*)가 사라진다.
 *
 * 세계 크기에서 유도한다 — `GRID_W` 를 바꾸면 공원도 따라 움직인다. 좌표를 직접 적으면
 * 격자를 줄이는 날 공원만 세계 밖에 남는다(이 파일 머리말이 경고하는 바로 그 형태다).
 */
export const PARK_X: readonly [number, number] = [-2, 1];
export const PARK_Z: readonly [number, number] = [GRID_MIN_Z + 2, -PLAZA_R - 1];

/** 이 파셀이 센트럴파크인가 */
export function isCentralPark(px: number, pz: number): boolean {
  return px >= PARK_X[0] && px <= PARK_X[1] && pz >= PARK_Z[0] && pz <= PARK_Z[1];
}

/**
 * 공원 안의 **호수 3영역**. 남에서 북으로 연못·호수·저수지.
 *
 * `pz` 는 공원 남쪽 끝에서의 상대 위치로 잡는다 — 공원이 움직이면 호수도 함께 움직인다.
 * 크기를 다르게 둔 것은 의도다: 셋이 같은 크기면 물웅덩이 셋으로 보이지 파크 안의 서로
 * 다른 수면으로 안 읽힌다.
 *
 * ⚠️ **파셀보다 정밀한 판정은 그릴 방법이 없다**(`decide/water.ts` 의 `parcelWater`
 * 주석). 지면이 파셀 한 칸을 통째로 덮는 판이므로 호수의 최소 단위도 파셀이다.
 * 그래서 부드러운 호숫가 곡선은 여기서 만들 수 없다 — 만들려면 지면 표현부터 바꿔야 한다.
 */
// ⚠️ **세 영역 사이에는 잔디가 최소 한 줄 있어야 한다.** `parcelWater` 가 물을 파셀
//    단위로 판정하므로, 두 호수가 이웃 칸에 닿으면 화면에서도 판정에서도 **한 덩어리**가
//    된다 — 감독이 요구한 *"호수 3개 영역"* 이 2개가 된다. `dz` 를 손볼 때 이 간격부터
//    확인하라(`tests/world5-water.test.ts` 가 `parcelWater` 축으로 센다).
//
// ⚠️ **`dz = 0`(광장에 맞닿는 줄)은 비워 둔다.** 실측에서 그 줄에 연못을 놓았더니
//    중앙 광장 북쪽 칸 `(0,−1)` — 시계탑이 서는 자리 — 이 `'shore'` 가 됐다. 배치는
//    물을 모르므로 무엇이 무너지지는 않지만, 미니맵에서 광장 한 칸이 물가 색으로 뜨고
//    "광장 전체가 뭍" 이라는 world2 부터의 단언이 깨진다. 잔디 한 줄을 밟고 들어가서
//    연못을 만나는 편이 *"광장에서 공원으로 들어선다"* 는 원래 의도에도 맞다.
const PARK_LAKES: readonly { x: readonly [number, number]; dz: readonly [number, number] }[] = [
  // 남쪽 연못 — 가장 작다(1칸). 공원에 들어서며 처음 만나는 물이다.
  { x: [0, 0], dz: [1, 1] },
  // 가운데 호수 — 가장 넓다(6칸). 공원의 중심이다. 공원 폭 4칸 중 3칸을 먹어 서쪽에만
  // 잔디가 남는데, 그 비대칭이 "물웅덩이" 가 아니라 호수로 읽히게 한다.
  { x: [-1, 1], dz: [3, 4] },
  // 북쪽 저수지 — 가로로 넓다(4칸 × 1줄). 실제 저수지가 공원 폭을 다 채운다.
  { x: [-2, 1], dz: [6, 6] },
];

/** 이 파셀이 공원 안 호수인가. 공원 밖이면 언제나 `false` */
export function isParkLake(px: number, pz: number): boolean {
  if (!isCentralPark(px, pz)) return false;
  // 공원 남쪽 끝에서 북쪽으로 센 거리. `PARK_Z[1]` 이 남쪽 끝이다(z 는 북이 음수).
  const dz = PARK_Z[1] - pz;
  return PARK_LAKES.some((l) => px >= l.x[0] && px <= l.x[1] && dz >= l.dz[0] && dz <= l.dz[1]);
}
