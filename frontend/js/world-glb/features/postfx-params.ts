// 후보정 파라미터 — **값만.** 여기에 three 의존이 없다.
//
// ── 왜 갈라 뒀나 ────────────────────────────────────────────────────────────
// `features/postfx.ts` 는 `three/webgpu` 와 `three/tsl` 을 import 한다. 그 파일을
// 테스트가 불러오면 노드 시스템이 통째로 로드되고, jsdom 에서는 그것이 터진다.
//
// 그런데 테스트가 꼭 확인해야 하는 것이 하나 있다 — **가로등이 블룸 문턱을 넘는가.**
// 그 관계가 어긋나서 블룸이 통째로 죽어 있었고(감독: *"가로등 똑같은데"*), 코드도
// 설정도 맞아 보이는데 두 숫자의 관계만 틀린 것이라 눈으로는 못 잡는다.
//
// 값만 이 파일로 빼면 테스트가 three 없이 그 관계를 검사할 수 있다.

/**
 * 이 밝기를 **넘는 픽셀만** 번진다.
 *
 * 등불색의 휘도(0.805)에 `LAMP_MAX_GLOW`(1.8)를 곱한 1.45 가 이 값을 넉넉히 넘어야
 * 가로등이 번지고, 동시에 이 값이 밤하늘 별·물 하이라이트보다 높아야 그것들은 안
 * 번진다. 그 양쪽을 `tests/world2-night.test.ts` 가 지킨다.
 */
export const BLOOM_THRESHOLD = 0.75;
